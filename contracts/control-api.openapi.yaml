openapi: 3.1.0
jsonSchemaDialect: https://json-schema.org/draft/2020-12/schema
info:
  title: AstrLink Local Control API
  version: 1.0.0
  summary: Stable local contract between the AstrLink desktop application and Core.
  description: |
    This API is a local control plane; it is not part of the public inference API.
    Core binds the inference plane to `127.0.0.1:8317` by default and binds this
    control plane to the ephemeral address `127.0.0.1:0`. On startup Core writes
    exactly one ready JSON line to stdout containing the selected `control_url`.
    Launchers must use that value rather than guessing a port.

    The bootstrap health, version, and capability operations are intentionally
    unauthenticated and disclose no configuration. Every operation that reads or
    mutates user configuration, events, or request records requires the per-start
    local control token. The token must be delivered through an inherited pipe or
    equivalent protected IPC; it must not be placed in process arguments or the
    environment.

    Request and response bodies are excluded from ordinary request records. The two
    audit-body switches are independent, default to false, and can only be enabled
    through this authenticated control API after explicit risk acknowledgement.

    The three bootstrap GET operations are always available. Authenticated Service
    CRUD, service-scoped subscription OAuth, priority Route CRUD, persistent local
    access-token operations, the fixed
    privacy policy, and selectable privacy-model catalog/installation management
    are available when Core is launched with its persistent data directory and a
    per-start control token delivered over stdin. The stable auto Route shape is
    rejected with 422 until the classifier runtime reaches its activation gates.
    Remaining paths freeze the forward control contract and stay marked
    `x-astrlink-implementation-status: planned`; their presence is not a runtime
    capability claim.
servers:
  - url: http://127.0.0.1:{control_port}
    description: Runtime loopback control address announced by Core on stdout.
    variables:
      control_port:
        default: "0"
        description: Ephemeral port; replace 0 with the value from the ready announcement.
security:
  - controlToken: []
x-astrlink-implementation:
  milestone: M3-unified-service-slice
  implemented_operations:
    - GET /control/v1/pricing/status
    - GET /control/v1/pricing/catalog
    - POST /control/v1/pricing/sync
    - GET /control/v1/pricing/summary
    - GET /control/v1/pricing/services/{service_id}
    - PUT /control/v1/pricing/services/{service_id}
    - POST /control/v1/pricing/services/{service_id}/backfill
    - GET /control/v1/health
    - GET /control/v1/version
    - GET /control/v1/capabilities
    - GET /control/v1/services
    - POST /control/v1/services
    - GET /control/v1/services/{service_id}
    - PATCH /control/v1/services/{service_id}
    - DELETE /control/v1/services/{service_id}
    - POST /control/v1/services/{service_id}/authorization
    - GET /control/v1/services/{service_id}/authorization
    - DELETE /control/v1/services/{service_id}/authorization
    - POST /control/v1/services/{service_id}/logout
    - POST /control/v1/services/{service_id}/probe-models
    - POST /control/v1/service-model-probes
    - POST /control/v1/service-proxy-probes
    - POST /control/v1/services/{service_id}/probe-responses
    - GET /control/v1/services/{service_id}/usage
    - POST /control/v1/services/{service_id}/usage/reset
    - GET /control/v1/access-tokens
    - GET /control/v1/access-token-usage
    - GET /control/v1/usage-summary
    - POST /control/v1/access-tokens
    - GET /control/v1/access-tokens/{token_id}/secret
    - DELETE /control/v1/access-tokens/{token_id}
    - GET /control/v1/policies
    - GET /control/v1/policies/{policy_id}
    - PATCH /control/v1/policies/{policy_id}
    - POST /control/v1/policies/{policy_id}/dry-run
    - GET /control/v1/privacy/regex-builtin-rules
    - GET /control/v1/privacy-model-catalog
    - POST /control/v1/privacy-models/probe
    - POST /control/v1/privacy-models/local/probe
    - GET /control/v1/privacy-models
    - POST /control/v1/privacy-models
    - GET /control/v1/privacy-models/{installation_id}
    - DELETE /control/v1/privacy-models/{installation_id}
    - GET /control/v1/requests
    - GET /control/v1/requests/{request_id}
    - DELETE /control/v1/requests/{request_id}
    - GET /control/v1/requests/{request_id}/children
    - POST /control/v1/requests/purge
    - GET /control/v1/requests/{request_id}/audit
    - GET /control/v1/audit-settings
    - PATCH /control/v1/audit-settings
    - POST /control/v1/auto-classifier/local/probe
    - GET /control/v1/auto-classifier
    - POST /control/v1/auto-classifier
    - POST /control/v1/auto-classifier/classify-preview
  planned_operations: all other operations in this document
x-astrlink-ready-announcement:
  transport: single JSON line on stdout after both listeners bind
  schema:
    $ref: "#/components/schemas/ReadyEvent"
tags:
  - name: Bootstrap
    description: Non-sensitive process bootstrap and compatibility handshake.
  - name: Services
    description: |
      The sole top-level API-service aggregate. Codex subscriptions and HTTP
      providers/gateways are service variants with independent IDs.
  - name: Access Tokens
    description: Persistent local inference credentials. Secrets are returned only by explicit create and reveal operations.
  - name: Routes
    description: Priority routing plus the reserved `astrlink/auto` virtual-model candidate pool.
  - name: Policies
    description: Content safety policies and privileged encrypted body-audit settings.
  - name: Privacy Model
    description: Integrity-checked catalog and lifecycle for selectable local privacy-model installations.
  - name: Auto Classifier
    description: Local-directory inspect, install, and dry-run classify for the frozen astrlink-text-v1 worker. An installed experimental or released artifact is routing-eligible.
  - name: Events
    description: Sanitized operational events.
  - name: Requests
    description: Request metadata and explicitly enabled encrypted audit content.
paths:
  /control/v1/pricing/status:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Services]
      operationId: getPricingStatus
      summary: Read official price synchronization status.
      responses:
        "200":
          description: Local reference pricing result, denominated in USD.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PricingStatus" }
        "400": { description: Invalid input. }
        "401": { description: Local control authentication required. }
        "404": { description: Service not found. }
        "502":
          {
            description: Price synchronization failed; last valid catalog retained.,
          }
  /control/v1/pricing/catalog:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Services]
      operationId: getPriceCatalog
      summary: Read canonical first-party API prices.
      responses:
        "200":
          description: Local reference pricing result, denominated in USD.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PriceCatalog" }
        "400": { description: Invalid input. }
        "401": { description: Local control authentication required. }
        "404": { description: Service not found. }
        "502":
          {
            description: Price synchronization failed; last valid catalog retained.,
          }
  /control/v1/pricing/sync:
    post:
      x-astrlink-implementation-status: implemented
      tags: [Services]
      operationId: syncOfficialPrices
      summary: Synchronize the official API price catalog.
      responses:
        "200":
          description: Local reference pricing result, denominated in USD.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PricingStatus" }
        "400": { description: Invalid input. }
        "401": { description: Local control authentication required. }
        "404": { description: Service not found. }
        "502":
          {
            description: Price synchronization failed; last valid catalog retained.,
          }
  /control/v1/pricing/summary:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Services]
      operationId: getBillingSummary
      summary: Aggregate retained call amounts in a half-open time range.
      parameters:
        - {
            name: from,
            in: query,
            required: true,
            schema: { type: string, format: date-time },
          }
        - {
            name: to,
            in: query,
            required: true,
            schema: { type: string, format: date-time },
          }
      responses:
        "200":
          description: Local reference pricing result, denominated in USD.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingSummary" }
        "400": { description: Invalid input. }
        "401": { description: Local control authentication required. }
        "404": { description: Service not found. }
        "502":
          {
            description: Price synchronization failed; last valid catalog retained.,
          }
  /control/v1/pricing/services/{service_id}:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Services]
      operationId: getServiceBilling
      summary: Read account-scoped official quota windows and monthly amounts.
      parameters:
        - name: service_id
          in: path
          required: true
          schema: { type: string, minLength: 3, maxLength: 96 }
      responses:
        "200":
          description: Local reference pricing result, denominated in USD.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ServiceBilling" }
        "400": { description: Invalid input. }
        "401": { description: Local control authentication required. }
        "404": { description: Service not found. }
        "502":
          {
            description: Price synchronization failed; last valid catalog retained.,
          }
    put:
      x-astrlink-implementation-status: implemented
      tags: [Services]
      operationId: configureServicePricing
      summary: Configure official provider mappings and a monthly reference budget.
      parameters:
        - name: service_id
          in: path
          required: true
          schema: { type: string, minLength: 3, maxLength: 96 }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PricingConfig" }
      responses:
        "200":
          description: Local reference pricing result, denominated in USD.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PricingConfig" }
        "400": { description: Invalid input. }
        "401": { description: Local control authentication required. }
        "404": { description: Service not found. }
        "502":
          {
            description: Price synchronization failed; last valid catalog retained.,
          }
  /control/v1/pricing/services/{service_id}/backfill:
    post:
      x-astrlink-implementation-status: implemented
      tags: [Services]
      operationId: backfillServicePricing
      summary: Value unpriced historical calls using current official prices.
      parameters:
        - name: service_id
          in: path
          required: true
          schema: { type: string, minLength: 3, maxLength: 96 }
      responses:
        "200":
          description: Local reference pricing result, denominated in USD.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingBackfill" }
        "400": { description: Invalid input. }
        "401": { description: Local control authentication required. }
        "404": { description: Service not found. }
        "502":
          {
            description: Price synchronization failed; last valid catalog retained.,
          }
  /control/v1/health:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Bootstrap]
      operationId: getHealth
      summary: Check whether Core is ready to serve the control API.
      security: []
      responses:
        "200":
          description: Core is ready.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: ok
        "503":
          description: Core is alive but not ready.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: starting
  /control/v1/version:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Bootstrap]
      operationId: getVersion
      summary: Read Core and contract versions for desktop compatibility checks.
      security: []
      responses:
        "200":
          description: Version handshake information.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Version"
              example:
                core_version: 0.1.0-dev
                control_api_version: v1
                protocol_contract_version: v1
                build_commit: unknown
  /control/v1/capabilities:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Bootstrap]
      operationId: getCapabilities
      summary: Read the protocol registry and execution-mode capabilities.
      description: |
        The Alpha response always declares native and delegated as available in
        Alpha. RelayKit remains visible as a future plan type, but
        `available_in_alpha` is false. Until RelayKit is integrated,
        `conversion_engine.available` is false, `version` is null, and `edges` is
        empty.
      security: []
      responses:
        "200":
          description: Protocol and planning capabilities.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: ./protocol-capabilities.schema.json
              example:
                protocol_contract_version: v1
                protocols:
                  - id: openai.responses
                    phase: alpha
                    primary: true
                    streaming: true
                  - id: openai.responses.compact
                    phase: alpha
                    primary: false
                    streaming: false
                  - id: anthropic.messages
                    phase: alpha
                    primary: false
                    streaming: true
                  - id: google.generate_content
                    phase: alpha
                    primary: false
                    streaming: true
                  - id: openai.chat
                    phase: alpha
                    primary: false
                    streaming: true
                  - id: openai.completions
                    phase: alpha
                    primary: false
                    streaming: true
                  - id: openai.models
                    phase: alpha
                    primary: false
                    streaming: false
                  - id: google.models
                    phase: alpha
                    primary: false
                    streaming: false
                plan_types:
                  - id: native
                    available_in_alpha: true
                    uses_local_conversion: false
                  - id: delegated
                    available_in_alpha: true
                    uses_local_conversion: false
                  - id: relaykit
                    available_in_alpha: false
                    uses_local_conversion: true
                conversion_engine:
                  name: relaykit
                  version: null
                  available: false
                  edges: []
  /control/v1/service-order:
    get:
      operationId: getServiceOrder
      summary: Read the complete global service priority order
      security:
        - controlToken: []
      responses:
        "200":
          description: All service IDs in priority order, including disabled services
          headers:
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceOrder"
    put:
      operationId: updateServiceOrder
      summary: Atomically replace the complete service order
      security:
        - controlToken: []
      parameters:
        - in: header
          name: If-Match
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceOrder"
      responses:
        "200":
          description: Saved order
          headers:
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceOrder"
        "412":
          description: Service membership or order has changed; reload before retrying
        "422":
          description: Duplicate, missing or unknown service IDs
        "428":
          description: If-Match is required
  /control/v1/services:
    x-astrlink-implementation-status: implemented
    get:
      tags: [Services]
      operationId: listServices
      summary: List every configured API service, including subscription and HTTP variants.
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: enabled
          in: query
          schema:
            type: boolean
        - name: kind
          in: query
          schema:
            $ref: "#/components/schemas/ServiceKind"
      responses:
        "200":
          description: Service page. Credential secrets and OAuth tokens are never returned.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServicePage"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Services]
      operationId: createService
      summary: Create one independently addressable API service.
      description: |
        `codex_subscription`, `claude_subscription` and `grok_subscription` create a
        disconnected service that is authorized through its service-scoped OAuth
        operation. HTTP kinds such as `newapi`
        carry an `http` connection and optional write-only credential.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceCreate"
      responses:
        "201":
          description: Service created.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ETag"
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /control/v1/services/{service_id}:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/ServiceId"
    get:
      tags: [Services]
      operationId: getService
      summary: Read one service without exposing credential material.
      responses:
        "200":
          description: Service details.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags: [Services]
      operationId: updateService
      summary: Update service metadata or an HTTP service connection.
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/ServicePatch"
      responses:
        "200":
          description: Updated service.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
    delete:
      tags: [Services]
      operationId: deleteService
      summary: Delete one service after route-reference checks.
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      responses:
        "204":
          description: Service and its local credential material were deleted.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /control/v1/services/{service_id}/authorization:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/ServiceId"
    post:
      tags: [Services]
      operationId: beginServiceAuthorization
      summary: Start provider-specific OAuth for this subscription service only.
      requestBody:
        required: false
        description: Omit the body only for legacy browser-flow callers; JSON bodies must include an explicit flow.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthorizationStartRequest"
      responses:
        "202":
          description: Service-scoped authorization session started.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthorizationSession"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    put:
      tags: [Services]
      operationId: completeServiceAuthorization
      summary: Exchange a Claude authorization code for the active service session.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [session_id, code]
              properties:
                session_id:
                  $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
                code:
                  type: string
                  minLength: 1
                  maxLength: 8192
                  writeOnly: true
                  description: Short-lived code#state from the registered Claude callback page. Never persisted or returned.
              additionalProperties: false
      responses:
        "200":
          description: Completed authorization session without credentials.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthorizationSession"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
    get:
      tags: [Services]
      operationId: getServiceAuthorization
      summary: Read the current OAuth session for this service.
      responses:
        "200":
          description: Authorization session without codes, verifiers, or tokens.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthorizationSession"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Services]
      operationId: cancelServiceAuthorization
      summary: Cancel the pending OAuth session for this service.
      responses:
        "200":
          description: Cancelled authorization session.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthorizationSession"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /control/v1/services/{service_id}/logout:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/ServiceId"
    post:
      tags: [Services]
      operationId: logoutService
      summary: Delete OAuth credentials while retaining the subscription service.
      responses:
        "200":
          description: Disconnected service.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /control/v1/services/{service_id}/probe-models:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/ServiceId"
    post:
      tags: [Services]
      operationId: probeServiceModels
      summary: Discover models through a persisted HTTP or subscription service.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [protocol]
              properties:
                protocol:
                  type: string
                  enum: [openai.models, google.models]
              additionalProperties: false
      responses:
        "200":
          description: Provider model list summary.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceModelProbe"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "502":
          $ref: "#/components/responses/BadGateway"
        "504":
          $ref: "#/components/responses/GatewayTimeout"
  /control/v1/service-proxy-probes:
    x-astrlink-implementation-status: implemented
    post:
      tags: [Services]
      operationId: probeServiceProxy
      summary: Test an unsaved custom proxy against an HTTP target.
      description: Sends a HEAD request with a 10-second timeout, without saving the draft, sending API credentials, or following redirects. An omitted proxy credential reuses the given service's stored proxy authentication; null clears it for this test only. An HTTP response other than 407 confirms a response was received, not API authorization.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [proxy, target_url]
              properties:
                service_id:
                  $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
                proxy:
                  allOf:
                    - $ref: "#/components/schemas/ServiceProxyInput"
                    - properties:
                        mode: { const: custom }
                target_url:
                  type: string
                  maxLength: 2048
                  description: HTTP or HTTPS target without userinfo, query or fragment.
              additionalProperties: false
      responses:
        "200":
          description: A response was received through the proxy.
          content:
            application/json:
              schema:
                type: object
                required: [latency_ms, status_code]
                properties:
                  latency_ms: { type: integer, minimum: 0 }
                  status_code:
                    {
                      type: integer,
                      minimum: 100,
                      maximum: 599,
                      not: { const: 407 },
                    }
                additionalProperties: false
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "405":
          description: Only POST is supported.
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "502":
          $ref: "#/components/responses/BadGateway"
  /control/v1/service-model-probes:
    x-astrlink-implementation-status: implemented
    post:
      tags: [Services]
      operationId: probeDraftServiceModels
      summary: Discover models using an unsaved HTTP service draft.
      description: A service_id allows an omitted draft credential to reuse that service's stored credential, even when the draft changes the HTTP URL or authentication scheme. An explicit draft credential takes precedence.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DraftServiceModelProbe"
      responses:
        "200":
          description: Upstream model identifiers without credential material.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceModelProbe"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "502":
          $ref: "#/components/responses/BadGateway"
        "504":
          $ref: "#/components/responses/GatewayTimeout"
  /control/v1/services/{service_id}/test:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/ServiceId"
    post:
      tags: [Services]
      operationId: testService
      summary: Send one real text generation request to a saved provider.
      description: |
        Uses saved credentials and a directly configured native or delegated
        protocol, without local conversion, retries, repair resends or failover. Disabled providers can
        be tested; subscriptions must be connected. May consume upstream quota.
        Codex subscriptions require stream=true. Times out after 60 seconds.
        Applies the current gateway privacy policy and response restoration, and
        writes request records using existing audit settings. Does not enable
        body capture or mutate provider configuration or routing health. Responses are
        bounded to 1 MiB. Returned text is redacted and limited to 4096 characters
        plus a truncation marker. Inference failures return HTTP 200 with ok=false.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceTestRequest"
      responses:
        "200":
          description: Test result with upstream status, duration and text output or failure detail.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTestResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "415":
          description: Content-Type must be application/json.
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "503":
          description: Provider testing is unavailable.
  /control/v1/services/{service_id}/probe-responses:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/ServiceId"
    post:
      tags: [Services]
      operationId: probeServiceResponses
      summary: Execute one non-streaming Responses probe through a connected subscription service.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                model:
                  type: string
                  minLength: 1
                  maxLength: 256
      responses:
        "200":
          description: Probe summary without upstream credential material.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceResponsesProbe"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "502":
          $ref: "#/components/responses/BadGateway"
  /control/v1/services/{service_id}/usage:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/ServiceId"
    get:
      tags: [Services]
      operationId: getServiceUsage
      summary: Read the live subscription rolling-quota snapshot.
      description: |
        Fetches the official ChatGPT `/wham/usage` snapshot for a connected
        `codex_subscription` service, Claude's `/api/oauth/usage` windows, or the
        Grok CLI proxy `/v1/billing?format=credits` snapshot. API-key coding
        plans (Kimi, GLM, MiniMax, OpenCode Go) report their plan windows with
        the service key, and a `newapi` service reports its key's prepaid
        `quota` from New API `/api/usage/token/`, valued at the site's
        `/api/status` quota per USD. The response is sanitized: email, user_id,
        account_id, key names, and credential material never appear. This
        lookup does not consume quota and does not redeem reset credits.
      responses:
        "200":
          description: Sanitized live usage snapshot.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionUsage"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "502":
          $ref: "#/components/responses/BadGateway"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
        "504":
          $ref: "#/components/responses/GatewayTimeout"
  /control/v1/services/{service_id}/usage/reset:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/ServiceId"
    post:
      tags: [Services]
      operationId: resetServiceUsage
      summary: Redeem one official Codex rate-limit reset credit.
      description: |
        Calls the official openai/codex consume path
        (`POST /wham/rate-limit-reset-credits/consume` on ChatGPT backend-api
        hosts, or `POST /api/codex/rate-limit-reset-credits/consume` otherwise)
        with a Core-generated `redeem_request_id`. The upstream `credit` object
        is dropped. Clients should refetch usage after `reset` or
        `already_redeemed`.
      responses:
        "200":
          description: Official consume outcome after a credit was applied.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionUsageReset"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "502":
          $ref: "#/components/responses/BadGateway"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
        "504":
          $ref: "#/components/responses/GatewayTimeout"
  /control/v1/usage-summary:
    x-astrlink-implementation-status: implemented
    get:
      tags: [Requests]
      operationId: getUsageSummary
      summary: Summarize all retained root requests in a calendar window.
      description: >-
        Returns successful request counts and tokens, failed request counts,
        and sparse calendar, service and model buckets in one response.
        Retry children, pending, blocked and cancelled requests are excluded.
        Legacy succeeded requests with HTTP errors count as failed. Service,
        model and local access-token buckets contain successful and failed request
        counts; token usage is accumulated only for successful requests. No
        pagination cap applies.
        Calendar buckets use the supplied IANA zone, including DST transitions.
      parameters:
        - name: from
          in: query
          required: true
          description: Inclusive whole-second RFC3339 timestamp.
          schema: { type: string, format: date-time }
        - name: to
          in: query
          required: true
          description: Exclusive whole-second RFC3339 timestamp, at most 366 days after from.
          schema: { type: string, format: date-time }
        - name: time_zone
          in: query
          required: true
          schema: { type: string, examples: [Asia/Tokyo] }
        - name: bucket
          in: query
          required: true
          description: Day buckets are always included; hour also includes local-hour buckets.
          schema: { type: string, enum: [day, hour] }
      responses:
        "200":
          description: Complete statistics for the retained window.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsageSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /control/v1/access-token-usage:
    x-astrlink-implementation-status: implemented
    get:
      tags: [Access Tokens]
      operationId: listAccessTokenUsage
      summary: Aggregate today and retained-history token counts for all local tokens.
      description: >-
        Counts successful root requests, excluding retry children and HTTP errors.
        Returns all attributed tokens with successful records, including deleted tokens;
        missing tokens have zero usage. Purging request history reduces these totals.
        Results are not paginated and contain no request details or token secrets.
      parameters:
        - name: today_from
          in: query
          required: true
          description: The caller's local midnight as an RFC3339 timestamp with whole-second precision; inclusive.
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Both totals for each attributed token in one snapshot.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessTokenUsageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /control/v1/access-tokens:
    x-astrlink-implementation-status: implemented
    get:
      tags: [Access Tokens]
      operationId: listAccessTokens
      summary: List persistent local inference access-token metadata.
      responses:
        "200":
          description: Access-token metadata. Secret values and hashes are never included.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessTokenList"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Access Tokens]
      operationId: createAccessToken
      summary: Create a persistent local inference access token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccessTokenCreate"
      responses:
        "201":
          description: Token created. The complete secret is returned only by this explicit operation and the reveal operation.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessTokenSecret"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /control/v1/access-tokens/{token_id}:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/AccessTokenId"
    delete:
      tags: [Access Tokens]
      operationId: deleteAccessToken
      summary: Permanently delete a local inference access token.
      responses:
        "204":
          description: Token metadata and secret were deleted. New inference requests using it are rejected immediately.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /control/v1/access-tokens/{token_id}/secret:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/AccessTokenId"
    get:
      tags: [Access Tokens]
      operationId: revealAccessToken
      summary: Explicitly reveal one persistent local inference access-token secret.
      responses:
        "200":
          description: Complete token secret. This response must not be logged or included in diagnostics.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessTokenReveal"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /control/v1/routes:
    x-astrlink-implementation-status: implemented
    get:
      tags: [Routes]
      operationId: listRoutes
      summary: List routes in deterministic evaluation order.
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: enabled
          in: query
          schema:
            type: boolean
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "200":
          description: Route page.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutePage"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Routes]
      operationId: createRoute
      summary: Create a route.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RouteCreate"
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "201":
          description: Route created.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ETag"
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Route"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /control/v1/routes/{route_id}:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/RouteId"
    get:
      tags: [Routes]
      operationId: getRoute
      summary: Read one route.
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "200":
          description: Route details.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Route"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags: [Routes]
      operationId: updateRoute
      summary: Update a route.
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/RoutePatch"
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "200":
          description: Updated route.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Route"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
    delete:
      tags: [Routes]
      operationId: deleteRoute
      summary: Delete a route.
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "204":
          description: Route deleted.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
  /control/v1/policies:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Policies]
      operationId: listPolicies
      summary: List the fixed MVP privacy policy.
      responses:
        "200":
          description: Policy page.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PolicyPage"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      x-astrlink-implementation-status: planned
      tags: [Policies]
      operationId: createPolicy
      summary: Create a policy.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PolicyCreate"
      responses:
        "201":
          description: Policy created.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ETag"
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Policy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /control/v1/policies/{policy_id}:
    parameters:
      - $ref: "#/components/parameters/PolicyId"
    get:
      x-astrlink-implementation-status: implemented
      tags: [Policies]
      operationId: getPolicy
      summary: Read one policy.
      responses:
        "200":
          description: Policy details.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Policy"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      x-astrlink-implementation-status: implemented
      tags: [Policies]
      operationId: updatePolicy
      summary: Update a policy.
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PolicyPatch"
      responses:
        "200":
          description: Updated policy.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Policy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
    delete:
      x-astrlink-implementation-status: planned
      tags: [Policies]
      operationId: deletePolicy
      summary: Delete a policy.
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      responses:
        "204":
          description: Policy deleted. Existing audit records are not implicitly deleted.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
  /control/v1/policies/{policy_id}/dry-run:
    parameters:
      - $ref: "#/components/parameters/PolicyId"
    post:
      x-astrlink-implementation-status: implemented
      tags: [Policies]
      operationId: dryRunPolicy
      summary: Preview privacy-policy evaluation against sample text.
      description: |
        Wraps sample_text into a minimal request body for the selected protocol,
        evaluates the effective policy (stored values merged with optional
        patchable overrides), and returns the decision plus finding locations.
        Matched plaintext is never returned. Does not forward upstream or mutate
        stored policy state. Only policy_privacy_default is supported.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PolicyDryRunRequest"
      responses:
        "200":
          description: Dry-run completed.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PolicyDryRunResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /control/v1/privacy/regex-builtin-rules:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Policies]
      operationId: listPrivacyRegexBuiltinRules
      summary: Read the built-in Regex detector catalog.
      description: |
        Returns the fixed kind and pattern pairs used by the builtin Regex
        detector. Intended to seed custom_regex_rules; the catalog itself is
        not editable. Custom mode compiles patterns only and does not reuse
        built-in validators such as Luhn or IBAN checks.
      responses:
        "200":
          description: Built-in Regex rules.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PolicyRegexBuiltinRulesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /control/v1/privacy-model-catalog:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Privacy Model]
      operationId: listPrivacyModelCatalog
      summary: List versioned built-in model choices and their CPU variants.
      responses:
        "200":
          description: Built-in catalog metadata. Model weights are not bundled.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyModelCatalog"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /control/v1/privacy-models/probe:
    post:
      x-astrlink-implementation-status: implemented
      tags: [Privacy Model]
      operationId: probePrivacyModel
      summary: Inspect a public Hugging Face repository without loading model weights.
      description: Content-derived identities matching `local/model-<12 lowercase hex>` are reserved for the mounted-directory probe and return a conflict here until that directory is probed again. Other repositories owned by `local` remain valid public repositories.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrivacyModelProbeRequest"
      responses:
        "200":
          description: Immutable revision, detected ONNX variants, and label-mapping requirements.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyModelProbe"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
        "502":
          $ref: "#/components/responses/BadGateway"
  /control/v1/privacy-models/local/probe:
    post:
      x-astrlink-implementation-status: implemented
      tags: [Privacy Model]
      operationId: probeLocalPrivacyModel
      summary: Inspect a model package from an already-mounted local path.
      description: The path may identify a package directory or one ONNX file within it. A file probe pins only that model variant plus its required config, tokenizer, and external data. The source path is used only for the authenticated probe and is never returned, logged, or persisted. Installation copies pinned assets into AstrLink private storage before execution.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrivacyModelLocalProbeRequest"
      responses:
        "200":
          description: Content-pinned local package identity, detected ONNX variants, and label-mapping requirements.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyModelProbe"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /control/v1/privacy-models:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Privacy Model]
      operationId: listPrivacyModelInstallations
      summary: List persistent model-and-variant installations and current progress.
      responses:
        "200":
          description: Installed, downloading, paused, or failed model variants.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyModelInstallationList"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      x-astrlink-implementation-status: implemented
      tags: [Privacy Model]
      operationId: installPrivacyModel
      summary: Start or retry one immutable model-and-variant installation.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrivacyModelInstallRequest"
      responses:
        "202":
          description: Download accepted.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyModelInstallation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
        "502":
          $ref: "#/components/responses/BadGateway"
  /control/v1/privacy-models/{installation_id}:
    parameters:
      - name: installation_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/PrivacyModelInstallationId"
    get:
      x-astrlink-implementation-status: implemented
      tags: [Privacy Model]
      operationId: getPrivacyModelInstallation
      summary: Read one installation and its current progress.
      responses:
        "200":
          description: Installation status.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyModelInstallation"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      x-astrlink-implementation-status: implemented
      tags: [Privacy Model]
      operationId: deletePrivacyModelInstallation
      summary: Cancel a download or remove one installed model variant.
      description: Deletion is rejected while the privacy policy references the installation.
      responses:
        "204":
          description: Download cancelled or installation removed.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /control/v1/privacy-models/{installation_id}/pause:
    parameters:
      - name: installation_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/PrivacyModelInstallationId"
    post:
      x-astrlink-implementation-status: implemented
      tags: [Privacy Model]
      operationId: pausePrivacyModelInstallation
      summary: Pause a model download and retain its downloaded files.
      responses:
        "200":
          description: Current installation status; a completed download remains ready.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyModelInstallation"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
        "502":
          $ref: "#/components/responses/BadGateway"
  /control/v1/privacy-models/{installation_id}/resume:
    parameters:
      - name: installation_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/PrivacyModelInstallationId"
    post:
      x-astrlink-implementation-status: implemented
      tags: [Privacy Model]
      operationId: resumePrivacyModelInstallation
      summary: Resume a paused or failed model download from retained files.
      responses:
        "200":
          description: Current installation status; a completed download remains ready.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyModelInstallation"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
        "502":
          $ref: "#/components/responses/BadGateway"
  /control/v1/auto-classifier/local/probe:
    post:
      x-astrlink-implementation-status: implemented
      tags: [Auto Classifier]
      operationId: probeLocalAutoClassifier
      summary: Inspect a sequence-classification package from an already-mounted local path.
      description: |
        The path identifies a package directory or one ONNX file within it. The
        source path is used only for the authenticated probe and is never
        returned, logged, or persisted. Installation copies pinned assets into
        AstrLink private storage before the worker may load them. Non-released
        artifact tiers stay ineligible for routing.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AutoClassifierLocalProbeRequest"
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "200":
          description: Content-pinned local package identity and frozen taxonomy mapping.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutoClassifierProbe"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /control/v1/auto-classifier:
    get:
      x-astrlink-implementation-status: implemented
      tags: [Auto Classifier]
      operationId: listAutoClassifierInstallations
      summary: List local classifier installations.
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "200":
          description: Installed classifier packages.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutoClassifierInstallationList"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      x-astrlink-implementation-status: implemented
      tags: [Auto Classifier]
      operationId: installAutoClassifier
      summary: Copy one probed local classifier package into private storage.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AutoClassifierInstallRequest"
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "202":
          description: Installation accepted and ready.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutoClassifierInstallation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /control/v1/auto-classifier/classify-preview:
    post:
      x-astrlink-implementation-status: implemented
      tags: [Auto Classifier]
      operationId: classifyPreviewAutoClassifier
      summary: Dry-run classify a text snippet or a protocol request body.
      description: |
        Control-plane only. The extracted current-user-text-v3 payload is not
        persisted, logged, or cached. Softmax is not returned and is not a
        routing gate. `eligible_for_routing` is true when a ready
        installation exists. Auto Route classifies `astrlink/auto` and
        fail-opens when the worker is unavailable.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AutoClassifierClassifyPreviewRequest"
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "200":
          description: Category, logits, latency, and routing eligibility.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutoClassifierClassifyPreviewResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /control/v1/recovery-paths:
    x-astrlink-implementation-status: implemented
    get:
      tags: [Routes]
      operationId: listRecoveryPaths
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "200":
          description: All reusable paths, versions, and references.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/RecoveryPathRecord"
                additionalProperties: false
    post:
      tags: [Routes]
      operationId: createRecoveryPath
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecoveryPathCreate"
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "201":
          description: Created path and strong version.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryPathRecord"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /control/v1/recovery-paths/{path_id}:
    x-astrlink-implementation-status: implemented
    parameters:
      - name: path_id
        in: path
        required: true
        schema:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
    get:
      tags: [Routes]
      operationId: getRecoveryPath
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "200":
          description: Path and its current references.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryPathRecord"
    patch:
      tags: [Routes]
      operationId: updateRecoveryPath
      parameters:
        - name: If-Match
          in: header
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/RecoveryPathPatch"
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "200":
          description: Updated path; active requests retain their original snapshot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryPathRecord"
        "412":
          description: The path version changed.
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
    delete:
      tags: [Routes]
      operationId: deleteRecoveryPath
      parameters:
        - name: If-Match
          in: header
          required: true
          schema: { type: string }
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "204":
          description: Path removed.
        "409":
          description: A route, category, or default still references this path.
        "412":
          description: The path version changed.
  /control/v1/recovery-paths/preview:
    x-astrlink-implementation-status: implemented
    post:
      tags: [Routes]
      operationId: previewRecoveryPath
      summary: Simulate the actual scheduler without saving, waiting, or sending inference requests.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecoveryPreviewInput"
      deprecated: true
      responses:
        "410":
          description: Legacy routing feature retired; use service order and routing settings
        "200":
          description: Actual attempt ordering and skipped steps for synthetic outcomes.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryPreview"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /control/v1/routing-settings:
    x-astrlink-implementation-status: implemented
    get:
      tags: [Routes]
      operationId: getRoutingSettings
      summary: Read failure handling for requests without a matching route.
      responses:
        "200":
          description: Persisted routing settings, applied to new requests.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutingSettings"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      tags: [Routes]
      operationId: updateRoutingSettings
      summary: Configure unmatched request retries and service switching.
      description: Switching defaults to false on installation and upgrade. Matching routes own their own target lists and are unaffected by this switch. In-flight requests retain their configuration snapshot.
      requestBody:
        required: true
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/RoutingSettingsPatch"
      responses:
        "200":
          description: Updated routing settings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutingSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /control/v1/audit-settings:
    x-astrlink-implementation-status: implemented
    get:
      tags: [Policies]
      operationId: getAuditSettings
      summary: Read the privileged global body-audit settings.
      description: M0 does not implement this planned operation. Both body switches default to false on installation and upgrade.
      responses:
        "200":
          description: Current audit settings. This response contains settings only, never captured content or encryption keys.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: ./protocol-capabilities.schema.json#/$defs/AuditSettings
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      tags: [Policies]
      operationId: updateAuditSettings
      summary: Update the privileged global body-audit settings.
      description: |
        M0 does not implement this planned operation. Enabling request-body or
        response-content capture requires `audit_risk_acknowledged=true` in the
        same request. Disabling capture immediately stops new body records and does
        not delete existing encrypted records.
      requestBody:
        required: true
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/AuditSettingsPatch"
      responses:
        "200":
          description: Updated audit settings.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: ./protocol-capabilities.schema.json#/$defs/AuditSettings
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /control/v1/events:
    x-astrlink-implementation-status: planned
    get:
      tags: [Events]
      operationId: listEvents
      summary: List sanitized operational events.
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/From"
        - $ref: "#/components/parameters/To"
        - name: severity
          in: query
          schema:
            $ref: "#/components/schemas/EventSeverity"
        - name: type
          in: query
          schema:
            type: string
            maxLength: 96
            pattern: "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)+$"
      responses:
        "200":
          description: Event page. Attributes are sanitized and never contain credentials or bodies.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /control/v1/requests:
    x-astrlink-implementation-status: implemented
    get:
      tags: [Requests]
      operationId: listRequestRecords
      summary: List root request metadata records.
      description: |
        Paginates root records only (parent_request_id null). Failed retry children
        are not listed here; fetch them via /requests/{request_id}/children.
        Service/status/protocol filters that match a child still surface its root
        group. Time ordering and cursors are based on the root. This operation
        never returns request or response body content.
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/From"
        - $ref: "#/components/parameters/To"
        - name: protocol
          in: query
          schema:
            $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
        - name: service_id
          in: query
          schema:
            $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        - name: local_access_token_id
          in: query
          description: Restrict to root records attributed to any of these local access tokens. Repeat the parameter for multiple token IDs.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 100
            uniqueItems: true
            items:
              $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/RequestStatus"
      responses:
        "200":
          description: Request-record page.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestRecordPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /control/v1/requests/{request_id}:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/RequestId"
    get:
      tags: [Requests]
      operationId: getRequestRecord
      summary: Read one request metadata record.
      responses:
        "200":
          description: Request metadata without body content.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestRecord"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Requests]
      operationId: deleteRequestRecord
      summary: Delete one metadata record and its encrypted audit content, if any.
      responses:
        "204":
          description: Record and associated encrypted audit blobs deleted.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /control/v1/requests/{request_id}/audit:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/RequestId"
    get:
      tags: [Requests]
      operationId: getRequestAuditContent
      summary: Decrypt and read explicitly captured audit content.
      description: |
        Available only through the authenticated local control plane. A null part
        means that direction was disabled or no content was captured. This response
        must not be logged, cached, included in diagnostics, or exported as ordinary
        configuration. Root records may expose client and upstream sides; retry
        children expose only their own upstream capture fields.
      responses:
        "200":
          description: Decrypted audit content.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditContent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /control/v1/requests/{request_id}/children:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/RequestId"
    get:
      tags: [Requests]
      operationId: listRequestRecordChildren
      summary: List failed retry attempts folded under a root request record.
      description: |
        Returns independent child request records for earlier upstream attempts,
        ordered by attempt_index ascending. The parent request_id must identify a
        root record (parent_request_id null). Each child remains addressable through
        the ordinary record and audit endpoints.
      responses:
        "200":
          description: Child request metadata without body content.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestRecordPage"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /control/v1/requests/purge:
    x-astrlink-implementation-status: implemented
    post:
      tags: [Requests]
      operationId: purgeRequestRecords
      summary: Explicitly delete request records and associated encrypted audit content.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PurgeRequest"
      responses:
        "200":
          description: Purge result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurgeResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /control/v1/request-sessions:
    x-astrlink-implementation-status: implemented
    get:
      tags: [Requests]
      operationId: listRequestSessions
      summary: List conversation sessions aggregated from root request records.
      description: |
        Groups root records that share a session_id. Roots with a null session_id
        appear as singleton sessions whose id is the request id. Does not return
        request or response body content.
      parameters:
        - name: kind
          in: query
          description: >
            Filters before pagination. Discovery includes openai.models and
            google.models; inference includes all other protocols. Omit for all.
          schema:
            type: string
            enum: [inference, discovery]
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/From"
        - $ref: "#/components/parameters/To"
        - name: protocol
          in: query
          schema:
            $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
        - name: service_id
          in: query
          schema:
            $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        - name: local_access_token_id
          in: query
          description: Restrict to sessions containing root records attributed to any of these local access tokens. Repeat the parameter for multiple token IDs.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 100
            uniqueItems: true
            items:
              $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        - name: status
          in: query
          description: >
            Keeps sessions that contain at least one root record with this
            status. It filters records, not the aggregated session outcome, so
            the returned status field may differ and SessionStatus values
            without a record counterpart are not accepted.
          schema:
            $ref: "#/components/schemas/RequestStatus"
      responses:
        "200":
          description: Request-session page.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestSessionPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /control/v1/request-sessions/{session_id}:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/SessionId"
    get:
      tags: [Requests]
      operationId: getRequestSession
      summary: Read one session summary and its root turns in time order.
      responses:
        "200":
          description: Session detail without body content.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestSessionDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /control/v1/request-sessions/{session_id}/channel-bindings:
    x-astrlink-implementation-status: implemented
    parameters:
      - $ref: "#/components/parameters/SessionId"
    get:
      tags: [Requests]
      parameters:
        - name: before
          in: query
          description: Read older audit events before this event id; active bindings are always current.
          schema: { type: integer, minimum: 1 }
      operationId: getSessionChannelBindings
      summary: Read active channel preferences and the latest 100 binding audit events.
      responses:
        "200":
          description: Local session channel bindings and audit history, without conversation text.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChannelBindingAudit"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Requests]
      operationId: releaseSessionChannelBindings
      summary: Release all ordinary channel preferences for this session and fence in-flight writes.
      description: Preserves request records, audit history and strict Responses or WebSocket bindings. The next ordinary request follows service order and may choose the same channel.
      responses:
        "200":
          description: Updated bindings and audit history.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChannelBindingAudit"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

components:
  securitySchemes:
    controlToken:
      type: http
      scheme: bearer
      bearerFormat: opaque-local-control-token
      description: Per-start local token delivered over protected IPC; never persist it in configuration or logs.
  parameters:
    Limit:
      name: limit
      in: query
      description: Maximum number of items to return.
      schema:
        type: integer
        minimum: 1
        maximum: 200
        default: 50
    Cursor:
      name: cursor
      in: query
      description: Opaque cursor returned by the previous page. It must not be parsed by clients.
      schema:
        type: string
        minLength: 1
        maxLength: 512
    From:
      name: from
      in: query
      description: Inclusive lower timestamp bound.
      schema:
        type: string
        format: date-time
    To:
      name: to
      in: query
      description: Exclusive upper timestamp bound.
      schema:
        type: string
        format: date-time
    ServiceId:
      name: service_id
      in: path
      required: true
      schema:
        $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
    AccessTokenId:
      name: token_id
      in: path
      required: true
      schema:
        $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
    RouteId:
      name: route_id
      in: path
      required: true
      schema:
        $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
    PolicyId:
      name: policy_id
      in: path
      required: true
      schema:
        $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
    RequestId:
      name: request_id
      in: path
      required: true
      schema:
        $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
    SessionId:
      name: session_id
      in: path
      required: true
      schema:
        $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
    IfMatch:
      name: If-Match
      in: header
      required: true
      description: ETag from the latest GET; prevents lost updates.
      schema:
        type: string
        minLength: 3
        maxLength: 128
  headers:
    NoStore:
      description: Sensitive local-control responses must not be cached.
      schema:
        type: string
        examples: [no-store]
    Location:
      description: Relative path of the created resource.
      schema:
        type: string
        format: uri-reference
    ETag:
      description: Strong entity tag for the current resource representation.
      schema:
        type: string
  responses:
    BadRequest:
      description: Malformed request, query, or cursor.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Missing or invalid local control token.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Resource not found.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: The operation conflicts with current state or references.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    PreconditionFailed:
      description: If-Match does not match the current resource representation.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    UnprocessableEntity:
      description: The request is well-formed but violates a contract or safety invariant.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    ServiceUnavailable:
      description: Local privacy policy or safety engine is temporarily unavailable.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InternalServerError:
      description: The local privacy-model operation or persistent state failed.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    BadGateway:
      description: Public model metadata could not be fetched or verified.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    GatewayTimeout:
      description: The upstream model-discovery request exceeded its deadline.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  schemas:
    PricingStatus:
      type: object
      required:
        [source, version, updated_at, checked_at, error, models, warnings]
      properties:
        source: { type: string }
        version: { type: string }
        updated_at: { type: [string, "null"], format: date-time }
        checked_at: { type: [string, "null"], format: date-time }
        error: { type: string }
        models: { type: integer, minimum: 0 }
        warnings: { type: array, items: { type: string } }
      additionalProperties: false
    OfficialPrice:
      type: object
      required: [provider, model, name, expression]
      properties:
        provider:
          {
            type: string,
            description: Canonical first-party supplier ID; plan and regional catalogs are excluded.,
          }
        model: { type: string }
        name: { type: string }
        expression:
          {
            type: string,
            description: Restricted tiered expression with USD per million token coefficients.,
          }
      additionalProperties: false
    PriceCatalog:
      type: object
      required: [version, generated_at, activated_at, etag, prices, warnings]
      properties:
        version: { type: string }
        generated_at: { type: string, format: date-time }
        activated_at: { type: string, format: date-time }
        etag: { type: string }
        prices:
          { type: array, items: { $ref: "#/components/schemas/OfficialPrice" } }
        warnings: { type: array, items: { type: string } }
      additionalProperties: false
    PricingConfig:
      type: object
      required: [provider, bindings, monthly_budget_usd, billing_day, time_zone]
      properties:
        provider:
          {
            type: string,
            description: Canonical first-party supplier ID; plan and regional catalogs are excluded.,
          }
        bindings:
          type: object
          additionalProperties:
            type: object
            required: [provider, model]
            properties:
              provider:
                {
                  type: string,
                  description: Canonical first-party supplier ID; plan and regional catalogs are excluded.,
                }
              model: { type: string, minLength: 1, maxLength: 256 }
            additionalProperties: false
        monthly_budget_usd:
          {
            type: string,
            description: Decimal USD reference budget; empty disables the budget.,
          }
        billing_day: { type: integer, minimum: 1, maximum: 31 }
        time_zone:
          { type: string, description: IANA calendar billing time zone. }
      additionalProperties: false
    BillingAmounts:
      type: object
      required: [amount_usd, priced, unpriced, pending, revalued, requests]
      properties:
        amount_usd: { type: string, pattern: '^[0-9]+\.[0-9]{9}$' }
        priced: { type: integer, minimum: 0 }
        unpriced: { type: integer, minimum: 0 }
        pending: { type: integer, minimum: 0 }
        revalued: { type: integer, minimum: 0 }
        requests:
          {
            type: integer,
            minimum: 0,
            description: Distinct logical root requests; attempts are deduplicated separately.,
          }
    BillingTokenGroup:
      allOf:
        - { $ref: "#/components/schemas/BillingAmounts" }
        - type: object
          required: [token_id]
          properties:
            token_id:
              $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
    BillingSummary:
      allOf:
        - { $ref: "#/components/schemas/BillingAmounts" }
        - type: object
          required: [from, to, by_model, by_token]
          properties:
            from: { type: string, format: date-time }
            to: { type: string, format: date-time }
            by_model:
              type: array
              items:
                allOf:
                  - { $ref: "#/components/schemas/BillingAmounts" }
                  - type: object
                    required: [model, provider]
                    properties:
                      model: { type: string }
                      provider: { type: string }
            by_token:
              type: array
              items:
                $ref: "#/components/schemas/BillingTokenGroup"
    BillingPeriod:
      type: object
      required:
        [
          id,
          kind,
          start,
          end,
          observed_at,
          used_percent,
          budget_usd,
          remaining_usd,
          coverage,
          summary,
        ]
      properties:
        id: { type: string }
        kind: { type: string }
        start: { type: string, format: date-time }
        end: { type: string, format: date-time }
        observed_at: { type: [string, "null"], format: date-time }
        used_percent: { type: [number, "null"], minimum: 0 }
        budget_usd: { type: string }
        remaining_usd:
          {
            type: string,
            description: May be negative on overrun; provisional while calls are unpriced or pending.,
          }
        coverage: { type: string, enum: [partial, observed, recorded] }
        summary: { $ref: "#/components/schemas/BillingSummary" }
      additionalProperties: false
    ServiceBilling:
      type: object
      required: [config, periods]
      properties:
        config: { $ref: "#/components/schemas/PricingConfig" }
        periods:
          { type: array, items: { $ref: "#/components/schemas/BillingPeriod" } }
      additionalProperties: false
    BillingBackfill:
      type: object
      required: [processed]
      properties:
        processed: { type: integer, minimum: 0 }
      additionalProperties: false
    ReadyEvent:
      type: object
      required:
        - event
        - core_version
        - control_api_version
        - protocol_contract_version
        - inference_url
        - control_url
      properties:
        event:
          type: string
          const: ready
        core_version:
          type: string
          minLength: 1
          maxLength: 64
        control_api_version:
          $ref: ./protocol-capabilities.schema.json#/$defs/ContractVersion
        protocol_contract_version:
          $ref: ./protocol-capabilities.schema.json#/$defs/ContractVersion
        inference_url:
          type: string
          format: uri
          pattern: "^http://127\\.0\\.0\\.1:(?:[1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])(?![\\s\\S])"
        control_url:
          type: string
          format: uri
          pattern: "^http://127\\.0\\.0\\.1:(?:[1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])(?![\\s\\S])"
      additionalProperties: false
    Health:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [ok, starting]
      additionalProperties: false
    UsageTotals:
      type: object
      required:
        [
          requests,
          failed_requests,
          input_tokens,
          output_tokens,
          total_tokens,
          cache_read_tokens,
          cache_write_tokens,
        ]
      properties:
        requests: { type: integer, minimum: 0 }
        failed_requests: { type: integer, minimum: 0 }
        input_tokens: { type: integer, minimum: 0 }
        output_tokens: { type: integer, minimum: 0 }
        total_tokens: { type: integer, minimum: 0 }
        cache_read_tokens: { type: integer, minimum: 0 }
        cache_write_tokens: { type: integer, minimum: 0 }
    UsageDayBucket:
      allOf:
        - $ref: "#/components/schemas/UsageTotals"
        - type: object
          required: [date]
          properties:
            date: { type: string, format: date }
      unevaluatedProperties: false
    UsageHourBucket:
      allOf:
        - $ref: "#/components/schemas/UsageTotals"
        - type: object
          required: [date, hour]
          properties:
            date: { type: string, format: date }
            hour: { type: integer, minimum: 0, maximum: 23 }
      unevaluatedProperties: false
    UsageGroup:
      allOf:
        - $ref: "#/components/schemas/UsageTotals"
        - type: object
          required: [id]
          properties:
            id: { type: [string, "null"] }
      unevaluatedProperties: false
    UsageSummary:
      type: object
      additionalProperties: false
      required: [totals, by_day, by_hour, by_service, by_model, by_token, scanned_records]
      properties:
        totals:
          $ref: "#/components/schemas/UsageTotals"
          unevaluatedProperties: false
        by_day:
          type: array
          items: { $ref: "#/components/schemas/UsageDayBucket" }
        by_hour:
          type: array
          items: { $ref: "#/components/schemas/UsageHourBucket" }
        by_service:
          type: array
          items: { $ref: "#/components/schemas/UsageGroup" }
        by_model:
          type: array
          items: { $ref: "#/components/schemas/UsageGroup" }
        by_token:
          type: array
          items: { $ref: "#/components/schemas/UsageGroup" }
        scanned_records: { type: integer, minimum: 0 }
    AccessTokenUsageResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [token_id, today_tokens, total_tokens]
            properties:
              token_id:
                $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
              today_tokens:
                type: integer
                format: int64
                minimum: 0
              total_tokens:
                type: integer
                format: int64
                minimum: 0
    AccessToken:
      type: object
      required: [id, name, hint, created_at]
      properties:
        id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        name:
          type: string
          minLength: 1
          maxLength: 64
        hint:
          type: string
          minLength: 1
          maxLength: 32
          description: Non-secret display hint that cannot be used to authenticate.
        created_at:
          type: string
          format: date-time
      additionalProperties: false
    AccessTokenCreate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 64
      additionalProperties: false
    AccessTokenSecret:
      type: object
      required: [token, access_token]
      properties:
        token:
          $ref: "#/components/schemas/AccessToken"
        access_token:
          type: string
          minLength: 48
          maxLength: 48
          pattern: "^astr_[A-Za-z0-9_-]{43}$"
      additionalProperties: false
    AccessTokenList:
      type: object
      required: [items, next_cursor]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/AccessToken"
        next_cursor:
          type: "null"
          description: Reserved for a future paginated contract; v1 currently returns the complete bounded list.
      additionalProperties: false
    AccessTokenReveal:
      type: object
      required: [access_token]
      properties:
        access_token:
          type: string
          minLength: 48
          maxLength: 48
          pattern: "^astr_[A-Za-z0-9_-]{43}$"
      additionalProperties: false
    Version:
      type: object
      required:
        - core_version
        - control_api_version
        - protocol_contract_version
        - build_commit
      properties:
        core_version:
          type: string
          minLength: 1
          maxLength: 64
        control_api_version:
          $ref: ./protocol-capabilities.schema.json#/$defs/ContractVersion
        protocol_contract_version:
          $ref: ./protocol-capabilities.schema.json#/$defs/ContractVersion
        build_commit:
          type: string
          minLength: 7
          maxLength: 64
          pattern: "^(?:[0-9a-f]{7,64}|unknown)$"
      additionalProperties: false
    FailureAction:
      type: string
      enum: [stop, retry, failover, retry_and_failover]
      description: Applies only to replayable requests before client response commitment. Retry and switching permissions are further bounded by the route and request budget.
    RecoveryPathNode:
      type: object
      required: [id, service_id, upstream_protocol, plan_type]
      properties:
        id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        service_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        upstream_protocol:
          $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
        plan_type:
          $ref: ./protocol-capabilities.schema.json#/$defs/PlanType
        upstream_model:
          type: string
          maxLength: 256
          description: Empty or omitted preserves the requested model.
        max_retries:
          type: integer
          minimum: 0
          maximum: 5
          description: Automatic targets only; omission inherits the effective policy.
      additionalProperties: false
    RecoveryPath:
      type: object
      required: [id, name, protocol, mode]
      properties:
        id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        name:
          type: string
          minLength: 1
          maxLength: 128
        protocol:
          $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
        mode:
          type: string
          enum: [automatic, steps]
        targets:
          type: array
          minItems: 1
          maxItems: 200
          items:
            $ref: "#/components/schemas/RecoveryPathNode"
        steps:
          type: array
          minItems: 1
          maxItems: 20
          items:
            $ref: "#/components/schemas/RecoveryPathNode"
          description: Same execution target may appear at most six times; node IDs remain unique.
        strategy:
          type: string
          enum: [retry_first, failover_first, failover_only]
        max_attempts:
          type: integer
          minimum: 1
          maximum: 20
        failure_policy:
          $ref: "#/components/schemas/FailurePolicy"
      oneOf:
        - properties:
            mode: { const: automatic }
          required: [targets]
          not:
            required: [steps]
        - properties:
            mode: { const: steps }
            steps:
              items:
                not:
                  required: [max_retries]
          required: [steps]
          not:
            anyOf:
              - required: [targets]
              - required: [strategy]
      additionalProperties: false
    RecoveryPathCreate:
      type: object
      required: [name, protocol, mode]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
        protocol:
          $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
        mode:
          type: string
          enum: [automatic, steps]
        targets:
          type: array
          minItems: 1
          maxItems: 200
          items:
            $ref: "#/components/schemas/RecoveryPathNode"
        steps:
          type: array
          minItems: 1
          maxItems: 20
          items:
            $ref: "#/components/schemas/RecoveryPathNode"
          description: Same execution target may appear at most six times; node IDs remain unique.
        strategy:
          type: string
          enum: [retry_first, failover_first, failover_only]
        max_attempts:
          type: integer
          minimum: 1
          maximum: 20
        failure_policy:
          $ref: "#/components/schemas/FailurePolicy"
      oneOf:
        - properties:
            mode: { const: automatic }
          required: [targets]
          not:
            required: [steps]
        - properties:
            mode: { const: steps }
            steps:
              items:
                not:
                  required: [max_retries]
          required: [steps]
          not:
            anyOf:
              - required: [targets]
              - required: [strategy]
      additionalProperties: false
    RecoveryPathPatch:
      type: object
      minProperties: 1
      properties:
        name:
          $ref: "#/components/schemas/RecoveryPath/properties/name"
        protocol:
          $ref: "#/components/schemas/RecoveryPath/properties/protocol"
        mode:
          $ref: "#/components/schemas/RecoveryPath/properties/mode"
        targets:
          oneOf:
            - $ref: "#/components/schemas/RecoveryPath/properties/targets"
            - type: "null"
        steps:
          oneOf:
            - $ref: "#/components/schemas/RecoveryPath/properties/steps"
            - type: "null"
        strategy:
          oneOf:
            - $ref: "#/components/schemas/RecoveryPath/properties/strategy"
            - type: "null"
        max_attempts:
          oneOf:
            - $ref: "#/components/schemas/RecoveryPath/properties/max_attempts"
            - type: "null"
        failure_policy:
          oneOf:
            - $ref: "#/components/schemas/RecoveryPath/properties/failure_policy"
            - type: "null"
      additionalProperties: false
    RecoveryPathRecord:
      type: object
      required: [path, etag, references]
      properties:
        path:
          $ref: "#/components/schemas/RecoveryPath"
        etag:
          type: string
          pattern: '^"sha256:[0-9a-f]{64}"$'
        references:
          type: array
          items:
            type: object
            required: [name, protocol, override]
            properties:
              route_id:
                $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
              name: { type: string, maxLength: 128 }
              protocol:
                $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
              category_id: { type: string, maxLength: 64 }
              override: { type: boolean }
            additionalProperties: false
      additionalProperties: false
    RecoveryPreviewInput:
      type: object
      required: [path, streaming, error]
      properties:
        path:
          $ref: "#/components/schemas/RecoveryPath"
        model: { type: string, maxLength: 256 }
        streaming: { type: boolean }
        error:
          type: string
          pattern: "^(network_error|response_timeout|[45][0-9]{2})$"
        success_at: { type: integer, minimum: 0, maximum: 20 }
        retry_after: { type: string, maxLength: 128 }
        route_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        category_id: { type: string, maxLength: 64 }
      additionalProperties: false
    RecoveryPreview:
      type: object
      required: [steps, stop_reason, max_attempts]
      properties:
        stop_reason: { type: string, maxLength: 128 }
        max_attempts: { type: integer, minimum: 1, maximum: 20 }
        steps:
          type: array
          items:
            type: object
            required:
              [
                step_id,
                service_id,
                model,
                action,
                status,
                wait_min_ms,
                wait_max_ms,
              ]
            properties:
              step_id:
                $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
              service_id:
                $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
              model: { type: string, maxLength: 256 }
              action: { type: string, enum: [initial, retry, failover] }
              status: { type: string, enum: [failed, succeeded, skipped] }
              reason: { type: string, maxLength: 128 }
              wait_min_ms: { type: integer, minimum: 0 }
              wait_max_ms: { type: integer, minimum: 0 }
            additionalProperties: false
      additionalProperties: false
    FailurePolicy:
      type: object
      required:
        [
          max_retries,
          initial_delay_ms,
          max_delay_ms,
          network_error,
          response_timeout,
          http_status,
        ]
      properties:
        max_retries:
          type: integer
          description: Ordinary retries per target, excluding the first request and independent request repairs. Zero does not disable repair.
          minimum: 0
          maximum: 5
          default: 1
        initial_delay_ms:
          type: integer
          minimum: 0
          maximum: 60000
          default: 500
        max_delay_ms:
          type: integer
          minimum: 0
          maximum: 60000
          default: 5000
          description: Must be at least initial_delay_ms. A longer Retry-After prevents retrying this target and permits switching only if the error rule allows it.
        response_start_timeout_seconds:
          type: integer
          minimum: 0
          maximum: 86400
          description: Omission uses the global timeout. Zero is unlimited. Never imposes a deadline on a started stream.
        thinking_signature_recovery:
          type: boolean
          default: true
          description: On a recognized Claude Messages thinking signature HTTP 400, repair assistant history and resend immediately to the same target. All request repairs share one repair per target, independent of failover strategy, max_retries, backoff and HTTP error rules, but count toward max_attempts. Manual paths repair within the current step without consuming a later step. Failed repairs follow ordinary error rules. Omission enables recovery; false disables it.
        openai_reasoning_recovery:
          type: boolean
          default: true
          description: On a recognized OpenAI Responses or Responses Compact reasoning HTTP 400, repair rejected reasoning ciphertext or identified item fields and resend immediately to the same target. Preserves summaries, tool pairs, previous_response_id and compaction context. All request repairs share one repair per target, independent of failover strategy, max_retries, backoff and HTTP error rules, but count toward max_attempts. Manual paths repair within the current step without consuming a later step. Failed repairs follow ordinary error rules. Omission enables recovery; false disables it.
        openai_function_output_recovery:
          type: boolean
          default: false
          description: Opt-in. On a recognized OpenAI Responses or Responses Compact HTTP 400 for encrypted function or custom-tool output, drop encrypted output parts, keep plaintext, and resend immediately to the same target. Shares one repair per target with reasoning repair and can clear both in the same attempt. Tool call_id pairs and compaction stay intact. Independent of failover strategy, max_retries, backoff and HTTP error rules, but counts toward max_attempts. Manual paths repair within the current step. Failed repairs follow ordinary error rules. Omission disables recovery; true enables it.
        network_error:
          $ref: "#/components/schemas/FailureAction"
        response_timeout:
          $ref: "#/components/schemas/FailureAction"
        http_status:
          type: object
          propertyNames:
            pattern: "^[45][0-9]{2}$"
          additionalProperties:
            $ref: "#/components/schemas/FailureAction"
          description: Controls ordinary retries and switching after any enabled request repair. Unlisted statuses stop. Request repair is independent of HTTP status rules. Defaults permit retry and switching for 408, 429, 500, 502, 503, 504, 529; 401 and 403 permit switching only.
      additionalProperties: false
      description: A complete policy. Missing service policy inherits RoutingSettings.default_failure_policy; missing route policy uses each service policy (or the global policy). A route override replaces the entire inherited policy. Missing route failover inherits global strategy and max_attempts with switching enabled.
    FailoverPolicy:
      type: object
      required: [enabled, strategy, max_attempts]
      properties:
        enabled:
          type: boolean
          default: true
        strategy:
          type: string
          enum: [retry_first, failover_first, failover_only]
          default: retry_first
        max_attempts:
          type: integer
          minimum: 1
          maximum: 20
          default: 6
          description: Total actual upstream inference requests, including the first request, ordinary retries, request repairs and backups.
      additionalProperties: false
    ServiceOrder:
      type: object
      additionalProperties: false
      required: [service_ids]
      properties:
        service_ids:
          type: array
          uniqueItems: true
          items:
            type: string
            pattern: "^[a-z][a-z0-9_-]{2,95}$"
    ChannelStickiness:
      type: object
      additionalProperties: false
      required: [enabled, ttl_seconds]
      properties:
        enabled: { type: boolean, default: true }
        ttl_seconds:
          { type: integer, minimum: 60, maximum: 86400, default: 3600 }
    ChannelBindingScope:
      type: object
      required: [session_id, local_access_token_id, protocol, model]
      properties:
        session_id: { type: string }
        local_access_token_id: { type: string }
        protocol: { type: string }
        model: { type: string }
    ChannelBinding:
      allOf:
        - $ref: "#/components/schemas/ChannelBindingScope"
        - type: object
          required: [service_id, source, request_id, updated_at, expires_at]
          properties:
            service_id: { type: string }
            source:
              { type: string, enum: [new, explicit, echo_id, fingerprint] }
            request_id: { type: string }
            updated_at: { type: string, format: date-time }
            expires_at: { type: string, format: date-time }
    ChannelBindingEvent:
      allOf:
        - $ref: "#/components/schemas/ChannelBindingScope"
        - type: object
          required:
            [
              id,
              at,
              action,
              reason,
              source,
              service_id,
              previous_service_id,
              request_id,
            ]
          properties:
            id: { type: integer, minimum: 1 }
            at: { type: string, format: date-time }
            action:
              {
                type: string,
                enum: [hit, miss, strict, bound, switched, released],
              }
            reason: { type: string }
            source: { type: string }
            service_id: { type: string }
            previous_service_id: { type: string }
            request_id: { type: string }
    ChannelBindingAudit:
      type: object
      additionalProperties: false
      required: [enabled, bindings, events, has_more]
      properties:
        enabled: { type: boolean }
        bindings:
          type: array
          items:
            $ref: "#/components/schemas/ChannelBinding"
        events:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/ChannelBindingEvent"
        has_more: { type: boolean }
    RoutingSettings:
      type: object
      required:
        [
          allow_unmatched_failover,
          strategy,
          max_attempts,
          default_failure_policy,
        ]
      properties:
        codex_identity_enforcement:
          type: boolean
          default: true
          description: Use one consistent Codex upstream identity. Changes apply to new requests without a gateway restart.
        claude_identity_enforcement:
          type: boolean
          default: true
          description: Use the default Claude client identity. When disabled, preserve recognized Claude client identities.
        grok_identity_enforcement:
          type: boolean
          default: true
          description: Use the default Grok client identity. When disabled, preserve recognized Grok client identities with a matching version header.
        channel_stickiness:
          $ref: "#/components/schemas/ChannelStickiness"
        default_recovery_paths:
          type: object
          additionalProperties:
            $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        default_failure_policy:
          $ref: "#/components/schemas/FailurePolicy"
        allow_unmatched_failover:
          type: boolean
          default: true
        strategy:
          type: string
          enum: [retry_first, failover_first, failover_only]
          default: retry_first
        max_attempts:
          type: integer
          minimum: 1
          maximum: 20
          default: 6
      additionalProperties: false
    RoutingSettingsPatch:
      type: object
      minProperties: 1
      properties:
        codex_identity_enforcement:
          type: boolean
        claude_identity_enforcement:
          type: boolean
        grok_identity_enforcement:
          type: boolean
        channel_stickiness:
          $ref: "#/components/schemas/ChannelStickiness"
        default_recovery_paths:
          type: object
          additionalProperties:
            $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        default_failure_policy:
          $ref: "#/components/schemas/FailurePolicy"
        allow_unmatched_failover:
          type: boolean
        strategy:
          type: string
          enum: [retry_first, failover_first, failover_only]
        max_attempts:
          type: integer
          minimum: 1
          maximum: 20
      additionalProperties: false
    RequestRecovery:
      type: object
      required: [delay_ms]
      properties:
        path_id: { type: string, maxLength: 128 }
        path_name: { type: string, maxLength: 128 }
        path_version: { type: string, maxLength: 128 }
        step_id: { type: string, maxLength: 128 }
        upstream_model:
          type: string
          maxLength: 256
          description: Actual model used by this attempt. Privileged local metadata; never injected into public inference errors.
        action:
          type: string
          enum: [retry, failover]
        reason:
          type: string
          maxLength: 128
        delay_ms:
          type: integer
          minimum: 0
          maximum: 60000
        stop_reason:
          type: string
          maxLength: 128
      additionalProperties: false
    ServiceKind:
      type: string
      enum:
        - codex_subscription
        - claude_subscription
        - grok_subscription
        - opencode_go
        - opencode_zen
        - kimi_coding
        - glm_coding
        - minimax_coding
        - deepseek
        - qwen
        - moonshot
        - glm
        - minimax
        - doubao
        - xai
        - newapi
        - openai
        - anthropic
        - gemini
        - openai_compatible
        - custom
      description: >
        Product-level service type. Codex, Claude and Grok subscriptions use OAuth.
        Coding plans and other HTTP services use provider-issued API keys.
    HTTPServiceConnection:
      type: object
      required: [base_url, auth]
      properties:
        base_url:
          type: string
          format: uri
          maxLength: 2048
          pattern: "^https?://[^?#]+$"
          not:
            pattern: "^https?://[^/]*@"
          description: HTTP(S) origin or reverse-proxy path prefix without userinfo, query, or fragment.
        auth:
          $ref: ./protocol-capabilities.schema.json#/$defs/ServiceAuth
        credential_ref:
          type: string
          pattern: "^local://service/[a-z][a-z0-9_-]{2,95}$"
          readOnly: true
          description: Opaque local credential reference; never contains credential bytes.
      additionalProperties: false
    SubscriptionServiceConnection:
      type: object
      required: [provider, status]
      properties:
        provider:
          type: string
          enum: [openai_codex, claude_code, xai_grok]
        status:
          type: string
          enum: [disconnected, authorizing, connected, needs_reauth, error]
        account_hint:
          type: string
          maxLength: 128
        provider_account_id:
          type: string
          maxLength: 256
        credential_ref:
          type: string
          maxLength: 512
          pattern: "^keyring://"
          readOnly: true
          description: Opaque keyring reference; OAuth token bytes never cross the control API.
        authorization_boundary:
          type: string
          maxLength: 240
        token_expires_at:
          type: string
          format: date-time
        last_refresh_at:
          type: string
          format: date-time
        last_error:
          $ref: "#/components/schemas/SubscriptionError"
      additionalProperties: false
    ServiceProxy:
      type: object
      required: [mode]
      properties:
        mode:
          type: string
          enum: [inherit, direct, custom]
        url:
          type: string
          maxLength: 2048
          description: HTTP, HTTPS or SOCKS5 proxy URL, without userinfo, query or fragment. Required in custom mode only.
        credential_ref:
          type: string
          readOnly: true
          description: Local proxy credential reference; never contains authentication bytes.
      additionalProperties: false
      allOf:
        - if:
            properties:
              mode: { const: custom }
          then:
            required: [url]
          else:
            not:
              anyOf:
                - required: [url]
                - required: [credential_ref]
    ServiceProxyInput:
      type: object
      required: [mode]
      properties:
        mode:
          type: string
          enum: [inherit, direct, custom]
        url:
          type: string
          maxLength: 2048
          description: HTTP, HTTPS or SOCKS5 proxy URL without embedded credentials; required for custom mode.
        credential:
          oneOf:
            - type: object
              required: [username, password]
              properties:
                username:
                  {
                    type: string,
                    minLength: 1,
                    maxLength: 255,
                    writeOnly: true,
                  }
                password: { type: string, maxLength: 255, writeOnly: true }
              additionalProperties: false
            - type: "null"
          writeOnly: true
          description: Omit to keep saved authentication; null clears it. Authentication requires custom mode. Credentials are limited to 255 UTF-8 bytes each.
      additionalProperties: false
      allOf:
        - if:
            properties:
              mode: { const: custom }
          then:
            required: [url]
          else:
            not:
              required: [url]
            properties:
              credential: { type: "null" }
    Service:
      type: object
      required:
        [id, name, kind, enabled, models, capabilities, created_at, updated_at]
      properties:
        proxy:
          $ref: "#/components/schemas/ServiceProxy"
        responses_websocket_enabled:
          type: boolean
          description: Per-channel native Responses WebSocket switch. When omitted, defaults to true for codex_subscription and false for other providers. Explicit false overrides the Codex default.
        failure_policy:
          $ref: "#/components/schemas/FailurePolicy"
        id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        name:
          type: string
          minLength: 1
          maxLength: 128
        kind:
          $ref: "#/components/schemas/ServiceKind"
        enabled:
          type: boolean
        models:
          $ref: "#/components/schemas/ServiceModels"
        capabilities:
          type: array
          items:
            $ref: ./protocol-capabilities.schema.json#/$defs/ServiceCapability
        http:
          $ref: "#/components/schemas/HTTPServiceConnection"
        subscription:
          $ref: "#/components/schemas/SubscriptionServiceConnection"
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
      additionalProperties: false
      oneOf:
        - properties:
            kind:
              enum: [codex_subscription, claude_subscription, grok_subscription]
          required: [subscription]
          not:
            required: [http]
        - properties:
            kind:
              $ref: ./protocol-capabilities.schema.json#/$defs/HTTPServiceKind
          required: [http]
          not:
            required: [subscription]
    HTTPServiceConnectionInput:
      type: object
      required: [base_url, auth]
      properties:
        base_url:
          type: string
          format: uri
          maxLength: 2048
          pattern: "^https?://[^?#]+$"
          not:
            pattern: "^https?://[^/]*@"
        auth:
          $ref: ./protocol-capabilities.schema.json#/$defs/ServiceAuth
        credential:
          $ref: "#/components/schemas/CredentialInput"
      additionalProperties: false
    ServiceCreate:
      type: object
      required: [name, kind]
      properties:
        proxy:
          oneOf:
            - $ref: "#/components/schemas/ServiceProxyInput"
            - type: "null"
          description: Omit to retain the saved proxy; null inherits the global network policy.
        responses_websocket_enabled:
          type: boolean
          description: Per-channel native Responses WebSocket switch. When omitted, defaults to true for codex_subscription and false for other providers. Explicit false overrides the Codex default.
        failure_policy:
          $ref: "#/components/schemas/FailurePolicy"
        name:
          type: string
          minLength: 1
          maxLength: 128
        kind:
          $ref: "#/components/schemas/ServiceKind"
        enabled:
          type: boolean
          default: true
        models:
          $ref: "#/components/schemas/ServiceModels"
        http:
          $ref: "#/components/schemas/HTTPServiceConnectionInput"
        capabilities:
          type: array
          items:
            $ref: ./protocol-capabilities.schema.json#/$defs/ServiceCapability
      additionalProperties: false
      oneOf:
        - properties:
            kind:
              enum: [codex_subscription, claude_subscription, grok_subscription]
          not:
            anyOf:
              - required: [http]
              - required: [capabilities]
        - properties:
            kind:
              $ref: ./protocol-capabilities.schema.json#/$defs/HTTPServiceKind
          required: [http, capabilities]
    HTTPServiceConnectionPatch:
      type: object
      minProperties: 1
      properties:
        base_url:
          type: string
          format: uri
          maxLength: 2048
          pattern: "^https?://[^?#]+$"
          not:
            pattern: "^https?://[^/]*@"
        auth:
          $ref: ./protocol-capabilities.schema.json#/$defs/ServiceAuth
        credential:
          oneOf:
            - $ref: "#/components/schemas/CredentialInput"
            - type: "null"
              description: Remove the stored HTTP credential.
      additionalProperties: false
    ServicePatch:
      type: object
      minProperties: 1
      properties:
        proxy:
          oneOf:
            - $ref: "#/components/schemas/ServiceProxyInput"
            - type: "null"
          description: Omit to retain the saved proxy; null inherits the global network policy.
        responses_websocket_enabled:
          type: boolean
          description: Per-channel native Responses WebSocket switch. When omitted, defaults to true for codex_subscription and false for other providers. Explicit false overrides the Codex default.
        failure_policy:
          oneOf:
            - $ref: "#/components/schemas/FailurePolicy"
            - type: "null"
        name:
          type: string
          minLength: 1
          maxLength: 128
        enabled:
          type: boolean
        models:
          $ref: "#/components/schemas/ServiceModels"
        http:
          $ref: "#/components/schemas/HTTPServiceConnectionPatch"
        capabilities:
          type: array
          items:
            $ref: ./protocol-capabilities.schema.json#/$defs/ServiceCapability
      additionalProperties: false
    CredentialInput:
      type: object
      required: [secret]
      properties:
        secret:
          type: string
          minLength: 1
          maxLength: 16384
          writeOnly: true
          description: Stored through CredentialStore and never returned or logged.
      additionalProperties: false
    ServiceModels:
      type: array
      maxItems: 2000
      uniqueItems: true
      items:
        type: string
        minLength: 1
        maxLength: 256
      default: []
      description: Exact service-wide model allow-list. An empty list makes the service unavailable for inference routing.
    DraftServiceModelProbe:
      type: object
      required: [kind, http, protocol]
      properties:
        proxy:
          oneOf:
            - $ref: "#/components/schemas/ServiceProxyInput"
            - type: "null"
          description: Omit to retain the saved proxy; null inherits the global network policy.
        service_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        kind:
          $ref: ./protocol-capabilities.schema.json#/$defs/HTTPServiceKind
        http:
          $ref: "#/components/schemas/HTTPServiceConnectionInput"
        protocol:
          type: string
          enum: [openai.models, google.models]
      additionalProperties: false
    AstrLinkAutoModel:
      type: string
      const: astrlink/auto
      description: >
        Reserved public virtual-model identifier. It is never an ordinary
        alias: requests using this exact model require a Route whose
        selection.mode is auto.
    RouteSelectionMode:
      type: string
      enum: [priority, auto]
      description: >
        priority chooses targets in deterministic target-priority order. auto
        applies hard capability constraints, classifies the bounded request into
        one category in a versioned taxonomy, keeps the targets configured under
        that category_id, and then uses target priority as the deterministic
        selection and fallback order. The classifier never predicts a target or
        upstream model identifier.
    RouteSelection:
      type: object
      required: [mode]
      properties:
        mode:
          $ref: "#/components/schemas/RouteSelectionMode"
        taxonomy_id:
          type: string
          minLength: 1
          maxLength: 64
          pattern: "^[a-z][a-z0-9._-]{0,63}$"
          example: astrlink-text-v1
          description: >
            Versioned task-classification taxonomy used by auto selection.
            Required when mode is auto and forbidden when mode is priority.
            It identifies label semantics, not a classifier implementation.
      allOf:
        - if:
            properties:
              mode:
                const: auto
            required: [mode]
          then:
            required: [taxonomy_id]
          else:
            not:
              required: [taxonomy_id]
      additionalProperties: false
      description: >
        Optional target-selection strategy. Omission is exactly equivalent to
        mode priority, preserving existing Route behavior. Mode auto requires
        match.model to equal astrlink/auto and replaces the ordinary top-level
        targets array with at least two RouteCategory objects. Every category
        owns one or more targets, every target has a concrete upstream_model,
        and the complete Route contains at least two distinct upstream model
        names. The taxonomy ID and category-owned model pools are stable
        configuration; classifier weights, confidence values, and model-ranking
        predictions are not.
    Route:
      type: object
      required:
        - id
        - name
        - enabled
        - priority
        - match
      properties:
        recovery_path_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        failure_policy:
          $ref: "#/components/schemas/FailurePolicy"
        failover:
          $ref: "#/components/schemas/FailoverPolicy"
        id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        name:
          type: string
          minLength: 1
          maxLength: 128
        enabled:
          type: boolean
        priority:
          type: integer
          minimum: 0
          maximum: 1000000
        match:
          $ref: "#/components/schemas/RouteMatch"
        selection:
          $ref: "#/components/schemas/RouteSelection"
        targets:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/RouteTarget"
        categories:
          type: array
          minItems: 2
          items:
            $ref: "#/components/schemas/RouteCategory"
      allOf:
        - if:
            properties:
              selection:
                properties:
                  mode:
                    const: auto
                required: [mode]
            required: [selection]
          then:
            required: [categories]
            properties:
              match:
                properties:
                  model:
                    $ref: "#/components/schemas/AstrLinkAutoModel"
                required: [model]
            not:
              anyOf:
                - required: [targets]
                - required: [recovery_path_id]
          else:
            oneOf:
              - required: [targets]
              - required: [recovery_path_id]
            properties:
              match:
                not:
                  properties:
                    model:
                      $ref: "#/components/schemas/AstrLinkAutoModel"
                  required: [model]
            not:
              required: [categories]
      additionalProperties: false
    RouteCreate:
      type: object
      required: [name, priority, match]
      properties:
        recovery_path_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        failure_policy:
          $ref: "#/components/schemas/FailurePolicy"
        failover:
          $ref: "#/components/schemas/FailoverPolicy"
        name:
          type: string
          minLength: 1
          maxLength: 128
        enabled:
          type: boolean
          default: true
        priority:
          type: integer
          minimum: 0
          maximum: 1000000
        match:
          $ref: "#/components/schemas/RouteMatch"
        selection:
          $ref: "#/components/schemas/RouteSelection"
        targets:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/RouteTarget"
        categories:
          type: array
          minItems: 2
          items:
            $ref: "#/components/schemas/RouteCategory"
      allOf:
        - if:
            properties:
              selection:
                properties:
                  mode:
                    const: auto
                required: [mode]
            required: [selection]
          then:
            required: [categories]
            properties:
              match:
                properties:
                  model:
                    $ref: "#/components/schemas/AstrLinkAutoModel"
                required: [model]
            not:
              anyOf:
                - required: [targets]
                - required: [recovery_path_id]
          else:
            oneOf:
              - required: [targets]
              - required: [recovery_path_id]
            properties:
              match:
                not:
                  properties:
                    model:
                      $ref: "#/components/schemas/AstrLinkAutoModel"
                  required: [model]
            not:
              required: [categories]
      additionalProperties: false
    RoutePatch:
      type: object
      minProperties: 1
      properties:
        recovery_path_id:
          oneOf:
            - $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
            - type: "null"
        failure_policy:
          oneOf:
            - $ref: "#/components/schemas/FailurePolicy"
            - type: "null"
        failover:
          oneOf:
            - $ref: "#/components/schemas/FailoverPolicy"
            - type: "null"
        name:
          type: string
          minLength: 1
          maxLength: 128
        enabled:
          type: boolean
        priority:
          type: integer
          minimum: 0
          maximum: 1000000
        match:
          $ref: "#/components/schemas/RouteMatch"
        selection:
          oneOf:
            - $ref: "#/components/schemas/RouteSelection"
            - type: "null"
              description: Restore the backward-compatible priority selection mode.
        targets:
          oneOf:
            - type: array
              minItems: 1
              items:
                $ref: "#/components/schemas/RouteTarget"
            - type: "null"
              description: Remove top-level priority targets when switching to auto.
        categories:
          oneOf:
            - type: array
              minItems: 2
              items:
                $ref: "#/components/schemas/RouteCategory"
            - type: "null"
              description: Remove category pools when switching to priority.
          description: >
            After applying a merge patch, the resulting Route must satisfy the
            priority/auto conditional shape and full cross-field validation.
      additionalProperties: false
    RouteMatch:
      type: object
      required: [protocol]
      properties:
        protocol:
          $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
        model:
          type: string
          minLength: 1
          maxLength: 256
          description: >
            Exact public model or alias. Omission matches any model.
            astrlink/auto is reserved and is valid only with
            selection.mode=auto.
      additionalProperties: false
    RouteTarget:
      type: object
      required:
        - service_id
        - plan_type
        - upstream_protocol
        - priority
      properties:
        service_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        plan_type:
          $ref: ./protocol-capabilities.schema.json#/$defs/PlanType
          description: RelayKit is a stable future value but is rejected while capabilities report it unavailable in Alpha.
        upstream_protocol:
          $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
        priority:
          type: integer
          minimum: 0
          maximum: 1000000
        upstream_model:
          type: string
          minLength: 1
          maxLength: 256
          description: >
            Rewrite the public model to this upstream model for this target.
            Requires a non-empty match.model on the route. Omission preserves
            the public model bytes. Every auto-selection target requires this
            field; target priority remains its deterministic low-confidence and
            transport-failure fallback order.
      additionalProperties: false
    RouteCategory:
      type: object
      required: [category_id]
      oneOf:
        - required: [targets]
        - required: [recovery_path_id]
      properties:
        recovery_path_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        category_id:
          type: string
          minLength: 1
          maxLength: 64
          pattern: "^[a-z][a-z0-9._-]{0,63}$"
          description: >
            One classifier output from selection.taxonomy_id. Category IDs are
            supplied by AstrLink's versioned taxonomy, not inferred from model
            names.
        targets:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/RouteTarget"
          description: >
            Ordered concrete model candidates configured under this category.
            Multiple models are allowed and target priority determines the
            category-local selection and transport-fallback order. The same
            concrete model may appear in another category explicitly.
      additionalProperties: false
    Policy:
      type: object
      required:
        - id
        - name
        - enabled
        - priority
        - detector
        - local_model_id
        - min_confidence
        - regex_source
        - custom_regex_rules
        - kind_rules
        - allowlist_rules
        - match
        - request_action
        - response_action
        - response_restore
        - restore_tool_arguments
        - placeholder_notice
      properties:
        id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        name:
          type: string
          minLength: 1
          maxLength: 128
        enabled:
          type: boolean
        priority:
          type: integer
          minimum: 0
          maximum: 1000000
        detector:
          $ref: "#/components/schemas/PolicyDetector"
        local_model_id:
          oneOf:
            - $ref: "#/components/schemas/PrivacyModelInstallationId"
            - type: "null"
          description: Required to reference a ready installation when detector is local_model; null for regex.
        min_confidence:
          type: number
          minimum: 0
          maximum: 1
          default: 0.8
          description: >
            Inclusive confidence threshold for local-model findings. Candidates
            below this value are suppressed before the request action is chosen.
            Regex findings do not use this threshold.
        match:
          $ref: "#/components/schemas/PolicyMatch"
        request_action:
          $ref: "#/components/schemas/PolicyAction"
        response_action:
          type: string
          const: allow
          readOnly: true
        response_restore:
          type: boolean
          default: true
          description: >
            When request_action is redact, restore this request's placeholders in
            the upstream response before returning to the client. Default true.
        regex_source:
          $ref: "#/components/schemas/PolicyRegexSource"
        custom_regex_rules:
          type: array
          maxItems: 64
          default: []
          items:
            $ref: "#/components/schemas/PolicyRegexRule"
          description: >
            Persisted even when regex_source is builtin so switching modes does
            not discard edits. Applied only when detector is regex and
            regex_source is custom; replaces the built-in Regex catalog.
        kind_rules:
          type: array
          items:
            $ref: "#/components/schemas/PolicyKindRule"
          description: >
            Normalized by Core to exactly one entry per PrivacyKind, so a
            partial patch never leaves the effective shape of a kind ambiguous.
        allowlist_rules:
          type: array
          maxItems: 128
          default: []
          items:
            $ref: "#/components/schemas/PolicyAllowlistRule"
          description: >
            Matched values are exempt from redaction and reach the upstream and
            the local agent unchanged.
        restore_tool_arguments:
          type: boolean
          default: true
          description: >
            Restore placeholders inside tool and function call arguments as well
            as visible text. The tool call runs in the operator's own agent
            harness, so it needs the real value; leaving this off while any kind
            uses the natural style lets a plausible-looking stand-in be written
            to disk or fetched over the network as if it were genuine.
        placeholder_notice:
          type: boolean
          default: true
          description: >
            When a request emits at least one token-shaped placeholder, prepend a
            short note to the upstream system instruction explaining that the
            markers are opaque and must be copied verbatim. The note never
            carries the mapping. Requests that emit only natural stand-ins skip
            it, since those need no explanation.
      additionalProperties: false
    PolicyKindRule:
      type: object
      required: [kind, enabled, style]
      properties:
        kind:
          $ref: "#/components/schemas/PrivacyKind"
        enabled:
          type: boolean
        style:
          $ref: "#/components/schemas/PlaceholderStyle"
      additionalProperties: false
      description: >
        Per-kind detection switch and placeholder shape. url and ip_address ship
        disabled because they are the dominant false-positive source for coding
        agents. common_secret, private_person, private_address, and private_date
        are rejected with any style other than token.
    PlaceholderStyle:
      type: string
      enum: [natural, token]
      default: token
      description: >
        token emits an opaque marker such as <PRIVATE_EMAIL_hex>: an unrestored
        leak is obvious, but the marker is out-of-distribution text that violates
        typed tool-argument schemas. natural emits a syntactically valid stand-in
        from a permanently reserved namespace, such as an address under the
        .invalid TLD: a model copies it without instruction and schemas accept
        it, but an unrestored leak looks plausible, so it is only safe alongside
        restore_tool_arguments.
    PolicyAllowlistRule:
      type: object
      required: [type, value]
      properties:
        type:
          type: string
          enum: [literal, domain_suffix, cidr]
        value:
          type: string
          minLength: 1
          maxLength: 256
          description: >
            An exact string for literal, a registrable suffix for domain_suffix,
            or CIDR notation for cidr. domain_suffix and cidr resolve the matched
            value to a host or address first, so one rule covers a bare host, a
            URL on that host, and an address at that domain.
      additionalProperties: false
    PrivacyKind:
      type: string
      description: Every kind a detector may emit; the local model adds the private_ kinds.
      enum:
        - common_secret
        - payment_card
        - account
        - email
        - phone
        - url
        - ip_address
        - private_person
        - private_address
        - private_date
    PolicyCreate:
      type: object
      required: [name, priority, detector, match, request_action]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
        enabled:
          type: boolean
          default: true
        priority:
          type: integer
          minimum: 0
          maximum: 1000000
        detector:
          $ref: "#/components/schemas/PolicyDetector"
        local_model_id:
          oneOf:
            - $ref: "#/components/schemas/PrivacyModelInstallationId"
            - type: "null"
        min_confidence:
          type: number
          minimum: 0
          maximum: 1
          default: 0.8
        match:
          $ref: "#/components/schemas/PolicyMatch"
        request_action:
          $ref: "#/components/schemas/PolicyAction"
        response_restore:
          type: boolean
          default: true
      additionalProperties: false
    PolicyPatch:
      type: object
      minProperties: 1
      properties:
        enabled:
          type: boolean
        detector:
          $ref: "#/components/schemas/PolicyDetector"
        local_model_id:
          oneOf:
            - $ref: "#/components/schemas/PrivacyModelInstallationId"
            - type: "null"
        min_confidence:
          type: number
          minimum: 0
          maximum: 1
        regex_source:
          $ref: "#/components/schemas/PolicyRegexSource"
        custom_regex_rules:
          type: array
          maxItems: 64
          items:
            $ref: "#/components/schemas/PolicyRegexRule"
        kind_rules:
          type: array
          items:
            $ref: "#/components/schemas/PolicyKindRule"
        allowlist_rules:
          type: array
          maxItems: 128
          items:
            $ref: "#/components/schemas/PolicyAllowlistRule"
        request_action:
          $ref: "#/components/schemas/PolicyAction"
        response_restore:
          type: boolean
        restore_tool_arguments:
          type: boolean
        placeholder_notice:
          type: boolean
      additionalProperties: false
    PolicyRegexSource:
      type: string
      enum: [builtin, custom]
      default: builtin
      description: >
        When detector is regex, builtin uses the fixed high-confidence catalog;
        custom uses only custom_regex_rules.
    PolicyRegexRule:
      type: object
      required: [kind, pattern]
      properties:
        kind:
          $ref: "#/components/schemas/PrivacyRegexDetectorKind"
        pattern:
          type: string
          minLength: 1
          maxLength: 512
          description: Go RE2 regular expression; compiled by Core on save and inspect.
      additionalProperties: false
    PolicyRegexBuiltinRulesResponse:
      type: object
      required: [rules]
      properties:
        rules:
          type: array
          items:
            $ref: "#/components/schemas/PolicyRegexRule"
      additionalProperties: false
    PrivacyRegexDetectorKind:
      type: string
      enum:
        - email
        - phone
        - account
        - payment_card
        - ip_address
        - url
        - common_secret
    PolicyDryRunRequest:
      type: object
      required: [protocol, sample_text]
      properties:
        protocol:
          $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
          description: Must be a protocol with inspectable request-body roots.
        sample_text:
          type: string
          minLength: 1
          maxLength: 262144
        policy:
          $ref: "#/components/schemas/PolicyPatch"
          description: Optional patchable overrides applied only for this evaluation.
      additionalProperties: false
    PolicyDryRunFinding:
      type: object
      required: [kind, path, start, end, confidence]
      description: >
        Matched span metadata only. Never includes matched plaintext; sample
        plaintext for placeholders appears solely on redactions for local dry-run.
      properties:
        kind:
          $ref: "#/components/schemas/PrivacyCanonicalKind"
        path:
          type: string
          minLength: 1
          description: JSON Pointer path of the inspected string segment.
        start:
          type: integer
          minimum: 0
        end:
          type: integer
          minimum: 1
        confidence:
          type: number
          minimum: 0
          maximum: 1
          description: Detector confidence retained from the local worker; Regex findings use 1.
        reason:
          type: string
          enum:
            - low_confidence
            - kind_disabled
            - allowlisted
            - placeholder
            - unrepresentable
          description: >
            Why the span was left in the request. Absent on accepted findings.
            placeholder means the value already belonged to a placeholder
            namespace, so redacting it again would make the innermost original
            unrecoverable. unrepresentable means the kind ran out of reserved
            stand-ins and fell back to a token placeholder.
      additionalProperties: false
    PolicyDryRunRedaction:
      type: object
      required: [placeholder, kind, value, style]
      properties:
        placeholder:
          type: string
          minLength: 1
        kind:
          $ref: "#/components/schemas/PrivacyCanonicalKind"
        value:
          type: string
          description: Original sample text for this placeholder; dry-run only.
        style:
          $ref: "#/components/schemas/PlaceholderStyle"
          description: >
            The shape actually emitted, which falls back to token when the kind's
            reserved namespace is exhausted.
      additionalProperties: false
    PolicyDryRunResponse:
      type: object
      required:
        [
          decision,
          findings_summary,
          findings,
          suppressed_findings,
          inspected_body,
        ]
      properties:
        decision:
          $ref: "#/components/schemas/PolicyAction"
        findings_summary:
          type: string
          description: Stable kind=count summary; never includes matched plaintext.
        findings:
          type: array
          items:
            $ref: "#/components/schemas/PolicyDryRunFinding"
          description: Findings that passed the configured threshold and drive the decision.
        suppressed_findings:
          type: array
          items:
            $ref: "#/components/schemas/PolicyDryRunFinding"
          description: >
            Detected spans that were left untouched, each carrying the reason.
            Covers disabled kinds, allowlist matches, values already inside a
            placeholder namespace, and local-model candidates below
            min_confidence.
        redactions:
          type: array
          items:
            $ref: "#/components/schemas/PolicyDryRunRedaction"
          description: Placeholder-to-sample mapping for local preview; never used on the live inference plane.
        redacted_body:
          type: string
          description: Present only when decision is redact.
        inspected_body:
          type: string
          description: Minimal request JSON that was actually inspected.
      additionalProperties: false
    PolicyMatch:
      type: object
      properties:
        protocols:
          type: array
          uniqueItems: true
          default: []
          items:
            $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
          description: Empty means all protocols.
        models:
          type: array
          uniqueItems: true
          default: []
          items:
            type: string
            minLength: 1
            maxLength: 256
          description: Empty means all public models and aliases.
        service_ids:
          type: array
          uniqueItems: true
          default: []
          items:
            $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
          description: Empty means all services.
      additionalProperties: false
    PolicyAction:
      type: string
      enum: [allow, warn, block, redact]
    PolicyDetector:
      type: string
      enum: [regex, local_model]
    PrivacyModelAdapter:
      type: string
      enum:
        [
          openai_bioes_viterbi,
          hf_token_classification,
          pplx_bioes_viterbi,
          astrlink_sensitive_guard,
        ]
    PrivacyModelInstallationId:
      type: string
      pattern: "^model_[0-9a-f]{32}$"
    PrivacyModelCatalogId:
      type: string
      pattern: "^catalog_[a-z0-9_]{3,80}$"
    PrivacyCanonicalKind:
      type: string
      enum:
        - email
        - phone
        - account
        - payment_card
        - ip_address
        - url
        - common_secret
        - private_address
        - private_date
        - private_person
    PrivacyModelVariant:
      type: object
      required:
        - id
        - name
        - quantization
        - bytes_total
        - estimated_ram_bytes
        - recommended
        - supported
        - unsupported_reason
      properties:
        id:
          type: string
          pattern: "^[a-z][a-z0-9_]{1,63}$"
        name:
          type: string
          minLength: 1
          maxLength: 64
          description: Trimmed display name; control characters are not allowed.
        quantization:
          type: string
          minLength: 1
          maxLength: 32
          description: Trimmed quantization identifier; control characters are not allowed.
        bytes_total:
          type: integer
          minimum: 0
          maximum: 9007199254740991
        estimated_ram_bytes:
          type: integer
          minimum: 0
          maximum: 9007199254740991
        recommended:
          type: boolean
        supported:
          type: boolean
        unsupported_reason:
          oneOf:
            - type: string
              enum: [cpu_only]
            - type: "null"
      additionalProperties: false
    PrivacyModelCatalogItem:
      type: object
      required:
        - id
        - name
        - summary
        - source
        - repo_id
        - revision
        - license
        - languages
        - adapter
        - variants
      properties:
        id:
          $ref: "#/components/schemas/PrivacyModelCatalogId"
        name:
          type: string
          minLength: 1
          maxLength: 128
          description: Trimmed model display name; control characters are not allowed.
        summary:
          type: string
          minLength: 1
          maxLength: 512
        source:
          type: string
          enum: [official, community]
        repo_id:
          type: string
          pattern: "^[A-Za-z0-9][A-Za-z0-9._-]{0,95}/[A-Za-z0-9][A-Za-z0-9._-]{0,95}$"
          description: Public Hugging Face repository ID. The exact synthetic shape `local/model-<12 lowercase hex>` is reserved for local-import identities.
        revision:
          type: string
          pattern: "^[0-9a-f]{40}$"
        license:
          type: string
          minLength: 1
          maxLength: 64
        languages:
          type: array
          maxItems: 32
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 64
        adapter:
          $ref: "#/components/schemas/PrivacyModelAdapter"
        variants:
          type: array
          minItems: 1
          maxItems: 32
          items:
            $ref: "#/components/schemas/PrivacyModelVariant"
      additionalProperties: false
    PrivacyModelCatalog:
      type: object
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/PrivacyModelCatalogItem"
      additionalProperties: false
    PrivacyModelProbeRequest:
      type: object
      required: [repo_id, revision]
      properties:
        repo_id:
          type: string
          pattern: "^[A-Za-z0-9][A-Za-z0-9._-]{0,95}/[A-Za-z0-9][A-Za-z0-9._-]{0,95}$"
          description: Public Hugging Face repository ID. The exact synthetic shape `local/model-<12 lowercase hex>` is reserved for local-import identities.
        revision:
          type: string
          minLength: 1
          maxLength: 128
          pattern: "^[A-Za-z0-9][A-Za-z0-9._/-]{0,127}$"
      additionalProperties: false
    PrivacyModelLocalProbeRequest:
      type: object
      required: [path]
      properties:
        path:
          type: string
          minLength: 1
          maxLength: 4096
          description: Absolute path to an already-mounted model package directory or `.onnx` file. URI forms such as smb:// are not accepted.
      additionalProperties: false
    PrivacyModelProbeLabel:
      type: object
      required: [label, suggested_kind]
      properties:
        label:
          type: string
          pattern: "^[A-Za-z0-9][A-Za-z0-9_.-]{0,127}$"
        suggested_kind:
          oneOf:
            - $ref: "#/components/schemas/PrivacyCanonicalKind"
            - type: "null"
        suggested_ignore:
          type: boolean
          default: false
          description: Explicit default to ignore this label. Only valid when suggested_kind is null; a null kind without this flag still requires user mapping.
      additionalProperties: false
    PrivacyModelProbe:
      type: object
      required:
        - repo_id
        - requested_revision
        - revision
        - name
        - license
        - languages
        - adapter
        - variants
        - labels
        - requires_label_mapping
      properties:
        repo_id:
          type: string
          pattern: "^[A-Za-z0-9][A-Za-z0-9._-]{0,95}/[A-Za-z0-9][A-Za-z0-9._-]{0,95}$"
        requested_revision:
          type: string
          minLength: 1
          maxLength: 128
          pattern: "^[A-Za-z0-9][A-Za-z0-9._/-]{0,127}$"
        revision:
          type: string
          pattern: "^[0-9a-f]{40}$"
        name:
          type: string
          minLength: 1
          maxLength: 128
          description: Trimmed model display name; control characters are not allowed.
        license:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 64
            - type: "null"
        languages:
          type: array
          maxItems: 32
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 64
        adapter:
          $ref: "#/components/schemas/PrivacyModelAdapter"
        variants:
          type: array
          minItems: 1
          maxItems: 32
          items:
            $ref: "#/components/schemas/PrivacyModelVariant"
        labels:
          type: array
          minItems: 1
          maxItems: 256
          items:
            $ref: "#/components/schemas/PrivacyModelProbeLabel"
        requires_label_mapping:
          type: boolean
          description: True when at least one label has neither a suggested kind nor an explicit suggested_ignore default.
      additionalProperties: false
    PrivacyModelLabelMapping:
      type: object
      maxProperties: 256
      propertyNames:
        pattern: "^[A-Za-z0-9][A-Za-z0-9_.-]{0,127}$"
      additionalProperties:
        oneOf:
          - $ref: "#/components/schemas/PrivacyCanonicalKind"
          - type: "null"
    PrivacyModelInstallRequest:
      type: object
      required: [repo_id, revision, variant_id, label_mapping]
      properties:
        repo_id:
          type: string
          pattern: "^[A-Za-z0-9][A-Za-z0-9._-]{0,95}/[A-Za-z0-9][A-Za-z0-9._-]{0,95}$"
        revision:
          type: string
          pattern: "^[0-9a-f]{40}$"
        variant_id:
          type: string
          pattern: "^[a-z][a-z0-9_]{1,63}$"
        label_mapping:
          $ref: "#/components/schemas/PrivacyModelLabelMapping"
      additionalProperties: false
    PrivacyModelInstallation:
      type: object
      required:
        - id
        - source
        - catalog_id
        - catalog_source
        - name
        - license
        - languages
        - repo_id
        - revision
        - variant_id
        - variant_name
        - quantization
        - adapter
        - status
        - bytes_downloaded
        - bytes_total
        - estimated_ram_bytes
        - error
        - label_mapping
        - installed_at
      properties:
        id:
          $ref: "#/components/schemas/PrivacyModelInstallationId"
        source:
          type: string
          enum: [catalog, custom, local]
        catalog_id:
          oneOf:
            - $ref: "#/components/schemas/PrivacyModelCatalogId"
            - type: "null"
        catalog_source:
          oneOf:
            - type: string
              enum: [official, community]
            - type: "null"
        name:
          type: string
          minLength: 1
          maxLength: 128
          description: Trimmed model display name; control characters are not allowed.
        license:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 64
            - type: "null"
        languages:
          type: array
          maxItems: 32
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 64
        repo_id:
          type: string
          pattern: "^[A-Za-z0-9][A-Za-z0-9._-]{0,95}/[A-Za-z0-9][A-Za-z0-9._-]{0,95}$"
        revision:
          type: string
          pattern: "^[0-9a-f]{40}$"
        variant_id:
          type: string
          pattern: "^[a-z][a-z0-9_]{1,63}$"
        variant_name:
          type: string
          minLength: 1
          maxLength: 64
          description: Trimmed variant display name; control characters are not allowed.
        quantization:
          type: string
          minLength: 1
          maxLength: 32
          description: Trimmed quantization identifier; control characters are not allowed.
        adapter:
          $ref: "#/components/schemas/PrivacyModelAdapter"
        status:
          type: string
          enum: [downloading, paused, ready, error]
        bytes_downloaded:
          type: integer
          minimum: 0
          maximum: 9007199254740991
        bytes_total:
          type: integer
          minimum: 0
          maximum: 9007199254740991
        estimated_ram_bytes:
          type: integer
          minimum: 0
          maximum: 9007199254740991
        error:
          oneOf:
            - type: string
              enum: [download_failed, integrity_failed, incompatible_model]
            - type: "null"
        label_mapping:
          $ref: "#/components/schemas/PrivacyModelLabelMapping"
        installed_at:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
      additionalProperties: false
    PrivacyModelInstallationList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/PrivacyModelInstallation"
      additionalProperties: false
    AutoClassifierInstallationId:
      type: string
      pattern: "^classifier_[0-9a-f]{32}$"
    AutoClassifierArtifactTier:
      type: string
      enum: [experimental, released]
    AutoClassifierLocalProbeRequest:
      type: object
      required: [path]
      properties:
        path:
          type: string
          minLength: 1
          maxLength: 4096
          description: Absolute path to an already-mounted classifier package directory or `.onnx` file.
      additionalProperties: false
    AutoClassifierInstallRequest:
      type: object
      required: [path]
      properties:
        path:
          type: string
          minLength: 1
          maxLength: 4096
      additionalProperties: false
    AutoClassifierProbe:
      type: object
      required:
        - repo_id
        - revision
        - name
        - artifact_tier
        - eligible_for_routing
        - taxonomy_id
        - taxonomy_sha256
        - bytes_total
        - id2label
      properties:
        repo_id:
          type: string
          minLength: 1
          maxLength: 128
        revision:
          type: string
          pattern: "^[0-9a-f]{40}$"
        name:
          type: string
          minLength: 1
          maxLength: 128
        artifact_tier:
          $ref: "#/components/schemas/AutoClassifierArtifactTier"
        eligible_for_routing:
          type: boolean
        taxonomy_id:
          type: string
          const: astrlink-text-v1
        taxonomy_sha256:
          type: string
          pattern: "^[0-9a-f]{64}$"
        bytes_total:
          type: integer
          minimum: 1
        id2label:
          type: array
          minItems: 4
          maxItems: 4
          items:
            type: string
            enum: [general, research, coding, architect]
      additionalProperties: false
    AutoClassifierInstallation:
      type: object
      required:
        - id
        - identity
        - name
        - artifact_tier
        - eligible_for_routing
        - taxonomy_id
        - status
        - bytes_total
      properties:
        id:
          $ref: "#/components/schemas/AutoClassifierInstallationId"
        identity:
          type: string
          minLength: 1
          maxLength: 256
        name:
          type: string
          minLength: 1
          maxLength: 128
        artifact_tier:
          $ref: "#/components/schemas/AutoClassifierArtifactTier"
        eligible_for_routing:
          type: boolean
        taxonomy_id:
          type: string
          const: astrlink-text-v1
        status:
          type: string
          enum: [ready]
        bytes_total:
          type: integer
          minimum: 1
      additionalProperties: false
    AutoClassifierInstallationList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          maxItems: 8
          items:
            $ref: "#/components/schemas/AutoClassifierInstallation"
      additionalProperties: false
    AutoClassifierClassifyPreviewRequest:
      type: object
      properties:
        text:
          type: string
          minLength: 1
          maxLength: 1048576
        protocol:
          $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
        body:
          type: object
          additionalProperties: true
      additionalProperties: false
    AutoClassifierClassifyPreviewResponse:
      type: object
      required: [latency_ms, artifact_tier, eligible_for_routing]
      properties:
        category:
          type: string
          enum: [general, research, coding, architect]
        logits:
          type: array
          minItems: 4
          maxItems: 4
          items:
            type: number
        latency_ms:
          type: integer
          minimum: 0
        artifact_tier:
          $ref: "#/components/schemas/AutoClassifierArtifactTier"
        eligible_for_routing:
          type: boolean
        fallback_reason:
          type: string
          enum: [empty_text, classifier_unavailable, timeout, invalid_text]
      additionalProperties: false
    AuditSettingsPatch:
      type: object
      minProperties: 1
      properties:
        request_body_enabled:
          type: boolean
        response_content_enabled:
          type: boolean
        http_meta_enabled:
          type: boolean
        request_body_max_bytes:
          type: integer
          minimum: 1024
          maximum: 16777216
        response_content_max_bytes:
          type: integer
          minimum: 1024
          maximum: 67108864
        metadata_retention_days:
          type: integer
          minimum: 1
          maximum: 3650
        content_retention_days:
          type: integer
          minimum: 1
          maximum: 365
        audit_risk_acknowledged:
          type: boolean
          writeOnly: true
          description: Must be true when enabling either capture switch; the acknowledgement is not persisted.
        extensions:
          oneOf:
            - $ref: ./protocol-capabilities.schema.json#/$defs/Extensions
            - type: "null"
          description: Namespaced extensions to replace, or null to delete them under RFC 7386 merge-patch semantics.
      additionalProperties: false
      allOf:
        - $ref: "#/components/schemas/AuditAcknowledgementInvariant"
    AuditAcknowledgementInvariant:
      if:
        anyOf:
          - required: [request_body_enabled]
            properties:
              request_body_enabled:
                const: true
          - required: [response_content_enabled]
            properties:
              response_content_enabled:
                const: true
      then:
        required: [audit_risk_acknowledged]
        properties:
          audit_risk_acknowledged:
            const: true
    EventSeverity:
      type: string
      enum: [debug, info, warning, error, critical]
    Event:
      type: object
      required: [id, occurred_at, type, severity, summary, subject, attributes]
      properties:
        id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        occurred_at:
          type: string
          format: date-time
        type:
          type: string
          minLength: 3
          maxLength: 96
          pattern: "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)+$"
          examples: [service.health_changed, core.started, policy.blocked]
        severity:
          $ref: "#/components/schemas/EventSeverity"
        summary:
          type: string
          minLength: 1
          maxLength: 512
          description: Sanitized human-readable text without upstream URLs, IPs, credentials, or body content.
        subject:
          $ref: "#/components/schemas/EventSubject"
        attributes:
          $ref: "#/components/schemas/SanitizedAttributes"
      additionalProperties: false
    EventSubject:
      type: object
      required: [type, id]
      properties:
        type:
          type: string
          enum: [core, service, route, policy, request]
        id:
          oneOf:
            - $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
            - type: "null"
      additionalProperties: false
    SanitizedAttributes:
      type: object
      maxProperties: 32
      propertyNames:
        pattern: "^[a-z][a-z0-9_]{0,63}$"
      additionalProperties:
        oneOf:
          - type: string
            maxLength: 512
          - type: number
          - type: boolean
          - type: "null"
      description: Bounded scalar metadata only. Bodies, headers, credentials, URLs with userinfo, and unsanitized upstream errors are forbidden.
    RequestStatus:
      type: string
      enum: [pending, succeeded, failed, cancelled, blocked]
    SessionStatus:
      type: string
      enum: [pending, succeeded, failed, cancelled, blocked, interrupted]
      description: >
        Outcome of a whole conversation, taken from its last call. interrupted
        has no record-level counterpart: the conversation delivered at least
        one successful answer and then the client abandoned the last stream,
        so a stopped agent loop is not reported as if nothing had run.
        cancelled is kept for a conversation that produced nothing.
    RequestRecord:
      type: object
      required:
        - id
        - parent_request_id
        - attempt_index
        - child_count
        - started_at
        - completed_at
        - status
        - input_protocol
        - requested_model
        - streaming
        - route_id
        - service_id
        - local_access_token_id
        - plan
        - http_status
        - latency_ms
        - usage
        - error
        - audit
        - privacy_restore
        - session_id
        - previous_response_id
        - output_response_id
        - input_preview
        - events
      properties:
        recovery:
          $ref: "#/components/schemas/RequestRecovery"
        id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        parent_request_id:
          oneOf:
            - $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
            - type: "null"
          description: |
            Null for the final upstream attempt (list root). Set to the root id for
            earlier failed retry attempts demoted to independent child records.
        attempt_index:
          type: integer
          minimum: 0
          description: |
            1-based index over real upstream RoundTrip attempts. 0 means the request
            never reached upstream (local blocked/failed). Legacy rows migrate as 1.
        child_count:
          type: integer
          minimum: 0
          description: Derived count of demoted retry children under a root; always 0 for children.
        started_at:
          type: string
          format: date-time
        completed_at:
          type: [string, "null"]
          format: date-time
        status:
          $ref: "#/components/schemas/RequestStatus"
        input_protocol:
          $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
        requested_model:
          type: [string, "null"]
          maxLength: 256
        reasoning_effort:
          type: [string, "null"]
          minLength: 1
          maxLength: 32
          description: Explicit client-requested reasoning level, normalized to lowercase. Null for unspecified levels and historical records; never inferred from model names or token budgets.
        streaming:
          type: boolean
        route_id:
          oneOf:
            - $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
            - type: "null"
        service_id:
          oneOf:
            - $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
            - type: "null"
        local_access_token_id:
          oneOf:
            - $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
            - type: "null"
          description: Stable local access-token attribution. The historical identifier remains after the token is deleted.
        plan:
          oneOf:
            - $ref: ./protocol-capabilities.schema.json#/$defs/ExecutionPlan
            - type: "null"
        http_status:
          type: [integer, "null"]
          minimum: 100
          maximum: 599
        latency_ms:
          type: [integer, "null"]
          minimum: 0
        usage:
          oneOf:
            - $ref: "#/components/schemas/Usage"
            - type: "null"
        error:
          oneOf:
            - $ref: "#/components/schemas/ErrorSummary"
            - type: "null"
        audit:
          $ref: "#/components/schemas/AuditRecordSummary"
        privacy_restore:
          oneOf:
            - $ref: "#/components/schemas/PrivacyRestoreSummary"
            - type: "null"
          description: Non-sensitive request-scoped placeholder restoration diagnostics; null for legacy records and requests without redactions.
        session_id:
          oneOf:
            - $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
            - type: "null"
          description: Conversation session this root belongs to. Null on legacy rows.
        previous_response_id:
          type: [string, "null"]
          minLength: 1
          maxLength: 256
          description: Inbound protocol cursor (previous_response_id, conversation_id, conversation.id, Claude metadata session, or prompt_cache_key).
        output_response_id:
          type: [string, "null"]
          minLength: 1
          maxLength: 256
          description: Client-facing response or message id extracted from the completed stream.
        input_preview:
          type: [string, "null"]
          minLength: 1
          maxLength: 80
          description: Bounded title from the newest visible user text. URLs and obvious secrets are stripped.
        turn_index:
          type: [integer, "null"]
          minimum: 1
          description: |
            1-based user turn within the session. Every model call of one agent
            loop shares the same value, so a session's turn_count can be far below
            its call_count. The first record of a session is turn 1 whatever its
            history holds; a later record starts a new turn when its history has
            more user messages than the record it links to, or the newest user
            text changed. Null when the protocol has no user turns or on legacy rows.
        turn_user_messages:
          type: [integer, "null"]
          minimum: 0
          description: |
            Number of user messages in this request's history (cumulative for
            stateful Responses chains). The next linked request compares its own
            count against this. Null whenever turn_index is null.
        turn_user_fingerprint:
          type: [string, "null"]
          minLength: 1
          maxLength: 256
          description: |
            Keyed fingerprint of this request's newest user text, compared by the
            next linked request to notice a new prompt at an unchanged message
            count (a harness that compacted history). Null without a fingerprint
            key or when turn_index is null. Reveals nothing about the text.
        session_link:
          oneOf:
            - $ref: "#/components/schemas/SessionLink"
            - type: "null"
          description: How this record joined an existing session; null when it started the session.
        cursors:
          type: [array, "null"]
          maxItems: 48
          items:
            $ref: "#/components/schemas/SessionCursor"
          description: |
            Typed values stored for this record: explicit cursors the request named
            (direction in) and everything the response produced (direction out).
            Fingerprints are keyed digests and reveal nothing about the text.
        events:
          type: array
          items:
            $ref: "#/components/schemas/RequestEvent"
          description: Gateway phase events for this attempt. Empty on legacy rows.
        extensions:
          $ref: ./protocol-capabilities.schema.json#/$defs/Extensions
      additionalProperties: false
    SessionCursorKind:
      type: string
      enum: [explicit, echo_id, fingerprint]
      description: |
        explicit: an id the client sent on purpose or the response's own id, matched globally.
        echo_id: an opaque id the model produced and the client echoed back, matched within the same access token and time window.
        fingerprint: keyed digest of the last assistant text, matched like echo_id.
    SessionCursor:
      type: object
      required: [kind, direction, value]
      properties:
        kind:
          $ref: "#/components/schemas/SessionCursorKind"
        direction:
          type: string
          enum: [in, out]
        value:
          type: string
          minLength: 1
          maxLength: 256
      additionalProperties: false
    SessionLink:
      type: object
      required: [kind, value]
      properties:
        kind:
          $ref: "#/components/schemas/SessionCursorKind"
        value:
          type: string
          minLength: 1
          maxLength: 256
      additionalProperties: false
    RequestEvent:
      type: object
      required: [kind, started_at, ended_at, status, summary, attempt_index]
      properties:
        kind:
          type: string
          enum: [accepted, privacy, routed, upstream, restore, completed]
        started_at:
          type: string
          format: date-time
        ended_at:
          type: [string, "null"]
          format: date-time
        status:
          $ref: "#/components/schemas/RequestStatus"
        summary:
          type: string
          maxLength: 256
        attempt_index:
          type: integer
          minimum: 0
      additionalProperties: false
    RequestSession:
      type: object
      required:
        - id
        - title
        - started_at
        - last_started_at
        - completed_at
        - duration_ms
        - active_request_starts
        - turn_count
        - call_count
        - status
        - requested_model
        - input_protocol
        - service_id
        - local_access_token_id
      properties:
        id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        title:
          type: string
          minLength: 1
          maxLength: 80
        started_at:
          type: string
          format: date-time
        last_started_at:
          type: string
          format: date-time
        completed_at:
          type: [string, "null"]
          format: date-time
        duration_ms:
          type: integer
          minimum: 0
          description: Cumulative recorded runtime of finished calls, including retry children, in milliseconds. Excludes gaps between calls and unknown runtime of requests recovered after a Core interruption.
        active_request_starts:
          type: array
          description: Start timestamps of calls still running without a recorded latency. Add their elapsed times to duration_ms for a live cumulative runtime.
          items:
            type: string
            format: date-time
        turn_count:
          type: integer
          minimum: 1
        call_count:
          type: integer
          minimum: 1
        status:
          $ref: "#/components/schemas/SessionStatus"
        requested_model:
          type: [string, "null"]
          maxLength: 256
        reasoning_effort:
          type: [string, "null"]
          minLength: 1
          maxLength: 32
          description: Explicit client-requested reasoning level, normalized to lowercase. Null for unspecified levels and historical records; never inferred from model names or token budgets.
        input_protocol:
          $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
        service_id:
          oneOf:
            - $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
            - type: "null"
        local_access_token_id:
          oneOf:
            - $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
            - type: "null"
      additionalProperties: false
    RequestSessionPage:
      type: object
      required: [items, next_cursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RequestSession"
        next_cursor:
          type: [string, "null"]
      additionalProperties: false
    RequestSessionDetail:
      allOf:
        - $ref: "#/components/schemas/RequestSession"
        - type: object
          required: [turns]
          properties:
            turns:
              type: array
              items:
                $ref: "#/components/schemas/RequestRecord"
          additionalProperties: false
    Usage:
      type: object
      description: |
        OpenAI-style input accounting. input_tokens includes all prompt-side
        tokens (uncached, cache read, cache write/creation, and multimodal
        input). cache_read_tokens / cache_write_tokens are optional subsets.
        Anthropic upstream fields are normalized by summing
        input + cache_read + cache_creation into input_tokens.
      required: [input_tokens, output_tokens, total_tokens]
      properties:
        input_tokens:
          type: integer
          minimum: 0
        output_tokens:
          type: integer
          minimum: 0
        total_tokens:
          type: integer
          minimum: 0
        cache_read_tokens:
          type: integer
          minimum: 0
        cache_write_tokens:
          type: integer
          minimum: 0
        cache_write_1h_tokens: { type: integer, minimum: 0 }
        input_audio_tokens: { type: integer, minimum: 0 }
        output_audio_tokens: { type: integer, minimum: 0 }
        thinking_enabled: { type: boolean }
        billing_incomplete:
          {
            type: boolean,
            description: The upstream stream ended before final usage was observed.,
          }
      additionalProperties: false
    ErrorSummary:
      type: object
      required: [category, code, message, retryable]
      properties:
        category:
          type: string
          minLength: 1
          maxLength: 64
          pattern: "^[a-z][a-z0-9_]*$"
        code:
          type: string
          minLength: 1
          maxLength: 96
          pattern: "^[a-z][a-z0-9_]*$"
        message:
          type: string
          minLength: 1
          maxLength: 1024
          description: Operator-visible failure text. Transport errors may include the upstream host, URL, and IP; credentials (Authorization, API keys, sensitive query values) are redacted. Never contains request or response bodies or header maps.
        retryable:
          type: boolean
      additionalProperties: false
    AuditRecordSummary:
      type: object
      required:
        - request_body_captured
        - response_content_captured
        - request_body_truncated
        - response_content_truncated
        - upstream_request_body_captured
        - upstream_response_content_captured
        - upstream_request_body_truncated
        - upstream_response_content_truncated
      properties:
        request_body_captured:
          type: boolean
        response_content_captured:
          type: boolean
        request_body_truncated:
          type: boolean
        response_content_truncated:
          type: boolean
        upstream_request_body_captured:
          type: boolean
        upstream_response_content_captured:
          type: boolean
        upstream_request_body_truncated:
          type: boolean
        upstream_response_content_truncated:
          type: boolean
      additionalProperties: false
    PrivacyRestoreSummary:
      type: object
      required: [enabled, mapping_count, restored_count, fallback_count]
      properties:
        enabled:
          type: boolean
        mapping_count:
          type: integer
          minimum: 0
        restored_count:
          type: integer
          minimum: 0
          description: Total placeholders restored across both channels.
        visible_restored_count:
          type: integer
          minimum: 0
          description: >
            Restored in text the reader sees. Zero on records written before the
            channel split, which report only restored_count.
        tool_argument_restored_count:
          type: integer
          minimum: 0
          description: >
            Restored inside tool or function call arguments. A non-zero value
            means the local agent harness was handed real values to act on, which
            is a materially different event from the model quoting one back.
        fallback_count:
          type: integer
          minimum: 0
        hits:
          type: array
          description: Request-time unique placeholder counts by kind. Absent on records written before this field existed.
          items:
            $ref: "#/components/schemas/PrivacyHitCount"
      additionalProperties: false
    PrivacyHitCount:
      type: object
      required: [kind, count]
      properties:
        kind:
          $ref: "#/components/schemas/PrivacyCanonicalKind"
        count:
          type: integer
          minimum: 1
      additionalProperties: false
    AuditContent:
      type: object
      required:
        - request_id
        - http_meta
        - request_body
        - response_content
        - upstream_http_meta
        - upstream_request_body
        - upstream_response_content
      properties:
        request_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        http_meta:
          oneOf:
            - $ref: "#/components/schemas/AuditHTTPMeta"
            - type: "null"
          description: Redacted client HTTP envelope (ADR 0008); null for children and when capture was disabled.
        request_body:
          oneOf:
            - $ref: "#/components/schemas/AuditContentPart"
            - type: "null"
          description: Original client request body before privacy/model/protocol rewrites; null on children.
        response_content:
          oneOf:
            - $ref: "#/components/schemas/AuditContentPart"
            - type: "null"
          description: Client-visible response after restore transforms; null on children.
        upstream_http_meta:
          oneOf:
            - $ref: "#/components/schemas/AuditHTTPMeta"
            - type: "null"
          description: |
            Redacted upstream envelope for this attempt. URL is path/query only —
            never an upstream origin. Credentials are masked at capture time.
        upstream_request_body:
          oneOf:
            - $ref: "#/components/schemas/AuditContentPart"
            - type: "null"
          description: Exact upstream request body after privacy, model rewrite, and RelayKit conversion.
        upstream_response_content:
          oneOf:
            - $ref: "#/components/schemas/AuditContentPart"
            - type: "null"
          description: Raw upstream response bytes before RelayKit/alias/privacy restoration.
      additionalProperties: false
    AuditHeader:
      type: object
      required: [name, value, redacted]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 256
        value:
          type: string
          maxLength: 2048
          description: Redacted entries carry a masked placeholder, never the original bytes.
        redacted:
          type: boolean
      additionalProperties: false
    AuditHTTPMeta:
      type: object
      required:
        [
          method,
          url,
          http_version,
          request_headers,
          response_status,
          response_headers,
        ]
      properties:
        method:
          type: string
          minLength: 1
          maxLength: 32
        url:
          type: string
          maxLength: 4096
          description: Userinfo stripped and sensitive query parameter values masked at capture time.
        http_version:
          type: string
          maxLength: 16
        request_headers:
          type: array
          maxItems: 128
          items:
            $ref: "#/components/schemas/AuditHeader"
        response_status:
          oneOf:
            - type: integer
              minimum: 100
              maximum: 599
            - type: "null"
        response_headers:
          type: array
          maxItems: 128
          items:
            $ref: "#/components/schemas/AuditHeader"
      additionalProperties: false
    AuditContentPart:
      type: object
      required: [media_type, content, truncated, captured_bytes]
      properties:
        media_type:
          type: string
          minLength: 1
          maxLength: 128
        content:
          type: string
          description: Decrypted content; never log, cache, diagnose, or include in ordinary exports.
        truncated:
          type: boolean
        captured_bytes:
          type: integer
          minimum: 0
      additionalProperties: false
    PurgeRequest:
      type: object
      required: [scope, confirm]
      properties:
        scope:
          type: string
          enum: [all, before]
        before:
          type: string
          format: date-time
          description: Required when scope is before.
        confirm:
          type: boolean
          const: true
          description: Explicit destructive-action confirmation.
      additionalProperties: false
      allOf:
        - if:
            properties:
              scope:
                const: before
            required: [scope]
          then:
            required: [before]
        - if:
            properties:
              scope:
                const: all
            required: [scope]
          then:
            not:
              required: [before]
    PurgeResult:
      type: object
      required: [deleted_records, deleted_audit_blobs]
      properties:
        deleted_records:
          type: integer
          minimum: 0
        deleted_audit_blobs:
          type: integer
          minimum: 0
      additionalProperties: false
    ServicePage:
      type: object
      required: [items, next_cursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Service"
        next_cursor:
          type: [string, "null"]
          maxLength: 512
      additionalProperties: false
    RoutePage:
      type: object
      required: [items, next_cursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Route"
        next_cursor:
          type: [string, "null"]
          maxLength: 512
      additionalProperties: false
    PolicyPage:
      type: object
      required: [items, next_cursor]
      properties:
        items:
          type: array
          maxItems: 1
          items:
            $ref: "#/components/schemas/Policy"
        next_cursor:
          type: "null"
      additionalProperties: false
    EventPage:
      type: object
      required: [items, next_cursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Event"
        next_cursor:
          type: [string, "null"]
          maxLength: 512
      additionalProperties: false
    RequestRecordPage:
      type: object
      required: [items, next_cursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RequestRecord"
        next_cursor:
          type: [string, "null"]
          maxLength: 512
      additionalProperties: false
    ErrorResponse:
      type: object
      required: [error, request_id]
      properties:
        error:
          $ref: "#/components/schemas/ControlError"
        request_id:
          type: string
          minLength: 1
          maxLength: 96
      additionalProperties: false
    ControlError:
      type: object
      required: [code, message, retryable, details]
      properties:
        code:
          type: string
          minLength: 1
          maxLength: 96
          pattern: "^[a-z][a-z0-9_]*$"
          examples: [missing_protocol_capability, validation_failed]
        message:
          type: string
          minLength: 1
          maxLength: 1024
          description: Sanitized user-facing message.
        retryable:
          type: boolean
        details:
          type: array
          maxItems: 32
          items:
            $ref: "#/components/schemas/ErrorDetail"
      additionalProperties: false
    ErrorDetail:
      type: object
      properties:
        field:
          type: string
          maxLength: 256
        reason:
          type: string
          maxLength: 512
        protocol:
          $ref: ./protocol-capabilities.schema.json#/$defs/ProtocolId
        service_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        required_plan_types:
          type: array
          uniqueItems: true
          items:
            $ref: ./protocol-capabilities.schema.json#/$defs/PlanType
      additionalProperties: false
    SubscriptionError:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          pattern: ^[a-z][a-z0-9_]{1,63}$
        message:
          type: string
          minLength: 1
          maxLength: 240
      additionalProperties: false
    AuthorizationFlow:
      type: string
      enum: [browser, device_code, authorization_code]
      description: Codex supports browser and device_code. Claude Code supports authorization_code with PKCE and a manual code#state callback. Grok (xai_grok) supports device_code only (RFC 8628 against auth.x.ai).
    AuthorizationStartRequest:
      type: object
      required: [flow]
      properties:
        flow:
          $ref: "#/components/schemas/AuthorizationFlow"
      additionalProperties: false
    AuthorizationDeviceCode:
      type: object
      required: [verification_url, user_code]
      properties:
        verification_url:
          type: string
          maxLength: 4096
        user_code:
          type: string
          minLength: 1
          maxLength: 128
      additionalProperties: false
    AuthorizationSession:
      type: object
      required:
        [
          id,
          provider,
          status,
          flow,
          service_id,
          expires_at,
          created_at,
          updated_at,
        ]
      properties:
        id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        provider:
          type: string
          enum: [openai_codex, claude_code, xai_grok]
        status:
          type: string
          enum: [pending, completed, cancelled, expired, failed]
        flow:
          $ref: "#/components/schemas/AuthorizationFlow"
        authorization_url:
          type: string
          maxLength: 4096
        device_code:
          $ref: "#/components/schemas/AuthorizationDeviceCode"
        service_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        expires_at:
          type: string
          format: date-time
        error:
          $ref: "#/components/schemas/SubscriptionError"
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
      allOf:
        - if:
            properties:
              status:
                const: pending
              flow:
                enum: [browser, authorization_code]
            required: [status, flow]
          then:
            required: [authorization_url]
            not:
              required: [device_code]
        - if:
            properties:
              status:
                const: pending
              flow:
                const: device_code
            required: [status, flow]
          then:
            required: [device_code]
            not:
              required: [authorization_url]
        - if:
            properties:
              status:
                enum: [completed, cancelled, expired, failed]
            required: [status]
          then:
            not:
              anyOf:
                - required: [authorization_url]
                - required: [device_code]
      additionalProperties: false
    ServiceModelProbe:
      type: object
      required: [protocol, model_ids]
      properties:
        service_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        protocol:
          type: string
          enum: [openai.models, google.models]
        model_ids:
          type: array
          maxItems: 10000
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 256
      additionalProperties: false
    ServiceTestProtocol:
      type: string
      enum:
        [
          openai.responses,
          openai.chat,
          openai.completions,
          anthropic.messages,
          google.generate_content,
        ]
    ServiceTestRequest:
      type: object
      additionalProperties: false
      required: [protocol, model]
      properties:
        protocol:
          $ref: "#/components/schemas/ServiceTestProtocol"
        model:
          type: string
          minLength: 1
          maxLength: 256
          description: Upstream ID with at most 256 UTF-8 bytes, no control characters or surrounding whitespace. May be outside the saved model list.
        stream:
          type: boolean
          default: false
        prompt:
          type: string
          maxLength: 2000
          description: Defaults to 'Reply with OK.' when omitted or blank.
    ServiceTestResult:
      type: object
      additionalProperties: false
      required:
        [
          service_id,
          protocol,
          model,
          stream,
          ok,
          status_code,
          duration_ms,
          output,
        ]
      properties:
        service_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        protocol:
          $ref: "#/components/schemas/ServiceTestProtocol"
        model:
          type: string
        stream:
          type: boolean
        ok:
          type: boolean
        status_code:
          type: integer
          minimum: 0
          maximum: 599
          description: Zero when no upstream response was received.
        duration_ms:
          type: integer
          minimum: 0
          description: Total test duration including credential preparation, privacy processing, reading, validation and request-record finalization.
        response_headers_ms:
          type: [integer, "null"]
          minimum: 0
          description: Milliseconds from sending the upstream request to receiving response headers, excluding credential preparation and request privacy processing. Null when headers were not received.
        first_token_ms:
          type: [integer, "null"]
          minimum: 0
          description: Milliseconds from sending the upstream request to the first non-empty visible SSE text after gateway processing. Excludes heartbeats and reasoning. Null for non-streaming responses or when no text arrived. Subtract response_headers_ms for the wait after headers.
        output:
          type: string
          maxLength: 4097
        raw_response:
          type: string
          maxLength: 65537
          description: Gateway-processed response body after configured privacy restoration, including actual JSON or SSE framing, local error envelopes and malformed responses. Credential values are redacted; limited to 65536 characters plus a truncation marker. Empty when no body was received. Older Core versions may omit this field.
        raw_response_truncated:
          type: boolean
          description: True if the body exceeded the size limit or could not be fully read.
        response_content_type:
          type: string
          maxLength: 257
          description: Credential-redacted gateway response Content-Type, when present.
        error_code:
          type: string
        message:
          type: string
          maxLength: 1001
    ServiceResponsesProbe:
      type: object
      required: [service_id, status_code, ok]
      properties:
        service_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        status_code:
          type: integer
          minimum: 100
          maximum: 599
        ok:
          type: boolean
        response_id:
          type: string
          maxLength: 256
      additionalProperties: false
    RateLimitWindow:
      type: object
      required: [used_percent]
      additionalProperties: false
      properties:
        used_percent:
          type: number
          minimum: 0
          maximum: 1000
        limit_window_seconds:
          type: integer
          minimum: 1
          maximum: 31622400
        reset_at:
          type: string
          format: date-time
        reset_after_seconds:
          type: integer
          minimum: 0
          maximum: 31622400
    AdditionalRateLimit:
      type: object
      required: [limit_name]
      additionalProperties: false
      properties:
        limit_name:
          type: string
          minLength: 1
          maxLength: 128
        metered_feature:
          type: string
          minLength: 1
          maxLength: 128
        primary:
          $ref: "#/components/schemas/RateLimitWindow"
        secondary:
          $ref: "#/components/schemas/RateLimitWindow"
    UsageCredits:
      type: object
      required: [has_credits, unlimited]
      additionalProperties: false
      properties:
        has_credits:
          type: boolean
        unlimited:
          type: boolean
        balance:
          type: string
          maxLength: 32
    UsageQuota:
      description: |
        Prepaid API-key allowance valued in USD, reported by a New API key. An
        unlimited key only reports what it has spent.
      type: object
      required: [unlimited, used_usd]
      additionalProperties: false
      properties:
        unlimited:
          type: boolean
        used_usd:
          type: string
          pattern: '^[0-9]{1,15}(\.[0-9]{1,6})?$'
        remaining_usd:
          description: Present only for a limited key.
          type: string
          pattern: '^[0-9]{1,15}(\.[0-9]{1,6})?$'
        total_usd:
          description: Present only for a limited key; used plus remaining.
          type: string
          pattern: '^[0-9]{1,15}(\.[0-9]{1,6})?$'
        expires_at:
          type: string
          format: date-time
    RateLimitResetCredits:
      type: object
      required: [available_count]
      additionalProperties: false
      properties:
        available_count:
          type: integer
          minimum: 0
          maximum: 1000
    SubscriptionUsage:
      type: object
      required: [service_id, fetched_at]
      additionalProperties: false
      properties:
        service_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        fetched_at:
          type: string
          format: date-time
        plan_type:
          type: string
          maxLength: 64
        allowed:
          type: boolean
        limit_reached:
          type: boolean
        primary:
          $ref: "#/components/schemas/RateLimitWindow"
        secondary:
          $ref: "#/components/schemas/RateLimitWindow"
        additional_rate_limits:
          type: array
          maxItems: 16
          items:
            $ref: "#/components/schemas/AdditionalRateLimit"
        credits:
          $ref: "#/components/schemas/UsageCredits"
        quota:
          $ref: "#/components/schemas/UsageQuota"
        rate_limit_reset_credits:
          $ref: "#/components/schemas/RateLimitResetCredits"
    SubscriptionUsageReset:
      type: object
      required: [service_id, outcome]
      additionalProperties: false
      properties:
        service_id:
          $ref: ./protocol-capabilities.schema.json#/$defs/ResourceId
        outcome:
          type: string
          enum: [reset, nothing_to_reset, no_credit, already_redeemed]
        windows_reset:
          type: integer
          minimum: 0
